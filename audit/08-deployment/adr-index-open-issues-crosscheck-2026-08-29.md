# ADR / 决策登记 / 未决事项三向一致性复核

- 复核日期：2026-08-29
- 复核时间：23:37–23:38 Asia/Shanghai
- 范围：`docs/09-decisions/ADR-*.md`、`docs/00-index/decision-register.md`、`docs/00-index/open-issues.md`
- 性质：只读复核；仅新增本报告和已授权的文档决策记录同步，不修改业务代码、部署配置或 Git 历史。

## 1. 结论

**三向登记一致性：通过。**

- ADR 文件：29 个；`ADR-0001` 至 `ADR-0029` 连续存在；
- ADR 元数据：29/29 具备状态、版本和决策来源；
- 决策登记表：29 行；DEC-001 至 DEC-029 无重复；
- 登记表链接：抽查/文件存在性通过；
- 未决事项：10 行均已标记已解决并关联 ADR-0020 至 ADR-0029；当前未解决项计数为 0。

这只证明“登记关系闭环”，不等于旧正文冲突已全部同步，也不等于实现代码、测试或部署已通过。

## 2. 三向映射

| 未决项 | 决策 | ADR | 状态 |
|---:|---|---|---|
| 1 | DEC-020 | ADR-0020 | 已解决 |
| 2 | DEC-021 | ADR-0021 | 已解决 |
| 3 | DEC-022 | ADR-0022 | 已解决 |
| 4 | DEC-023 | ADR-0023 | 已解决 |
| 5 | DEC-024 | ADR-0024 | 已解决 |
| 6 | DEC-025 | ADR-0025 | 已解决 |
| 7 | DEC-026 | ADR-0026 | 已解决 |
| 8 | DEC-027 | ADR-0027 | 已解决 |
| 9 | DEC-028 | ADR-0028 | 已解决 |
| 10 | DEC-029 | ADR-0029 | 已解决 |

## 3. ADR 元数据门禁

- 编号连续性：PASS；
- 状态字段：29/29 为 `accepted`；
- 版本字段：29/29 为 `v1.0.0`；
- 决策来源字段：29/29 存在；
- 决策登记：29/29 已进入 `decision-register.md`；
- 旧 ADR-0001～0019 与新增 ADR-0020～0029 的编号和登记无断档。

## 4. 规则重叠复核

### 4.1 退款

ADR-0003 与 ADR-0020 已统一为：退款状态为 `pending → approved → processing → completed`，消费/API 失败退款增加余额一次，充值订单退款走原路渠道，调账/人工上账纠错走独立红冲，当前版本禁止负余额。未发现相反正式规则，但旧 `docs/` 正文仍需逐条同步并标记 superseded。

### 4.2 API 路径

ADR-0005 与 ADR-0023 已统一 canonical/alias 及 v1.x 到 v2.0.0 的兼容窗口。错误的单层 `/api/v1/*` 历史路径仍可能存在于旧参考文档，属于正文同步任务，不是本登记层冲突。

### 4.3 迁移、备份与发布

ADR-0007、0014、0026、0028、0029 的层次关系一致：先环境/备份，再迁移，失败即停；custom dump 使用 `pg_restore`；发布基线须有完整元数据。实际 runner、备份任务、恢复演练和真实测试结果尚未形成证据，仍不能判发布通过。

## 5. 仍存在的下游工作

1. 将 ADR 规则逐条同步到旧正文或完成正式章节迁移；
2. 修复全仓旧文档链接和 API reference 路径；
3. 已清理 `user-agent-atomic.csv` 的 104 个 `? Phase 2` 占位，统一替换为 `MISSING`；这些条目仍不能计为已取得实现证据，需后续补真实证据；
4. 重做 15 份逻辑审计的模板断言、真实行号和测试证据；
5. 补齐首批页面的真实源码/API/DB/权限/测试/运行态证据；
6. 生成真实而非模板化的 `release-baseline.md`；
7. 在文档证据和真实测试门禁通过前，禁止业务代码修复和生产部署。

## 6. 边界确认

- 未修改业务代码；
- 未执行部署；
- 未提交 Git；
- 未推送远程仓库。
