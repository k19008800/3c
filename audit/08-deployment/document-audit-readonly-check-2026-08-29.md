
## 10. 第 2 批文档落地复核附录（23:03）

### 已新增

- 核心资金 PRD：6 个入口/主题文件；
- 核心资金 SPEC：6 个入口/主题文件；
- 状态机：充值、人工上账、调账、退款/红冲、结算/对账，共 5 个文件；
- 数据与架构：精度、数据字典、权限、事件通知、迁移、事务并发，共 6 个文件；
- API：统一约定、幂等、错误码、用户充值、管理端资金，共 5 个文件；
- 测试与验收：策略、验收标准、发布门禁，共 3 个文件；
- 运维与部署：环境、迁移、部署、备份恢复、进程端口、清单，共 6 个文件。

### 复核结果

- 新文档扫描文件数：68；
- 新文档相对链接缺失：0；
- ADR 文件：19 个，编号和元数据检查通过；
- 新增主题文档均为 `draft`，没有错误宣称为 `approved`；
- `CONFLICT/TBD` 已保留在未决事项和主题文件中，未将未裁决内容伪装成正式业务规则；
- 旧 `docs/` 正文未移动、删除或覆盖；业务代码、部署和 Git 未触碰。

### 当前门禁

- 文档目录与章节骨架：通过；
- 文档正文迁移与联合复核：未通过；
- P0/P1 冲突消解：未通过；
- 逻辑审计证据门禁：未通过；
- 页面审计证据门禁：未通过；
- 代码修复、生产部署：继续禁止。

## 11. API 路径同步记录（23:45）

- `docs/api-reference.md` 已按 ADR-0005/0023 修正已核实的 canonical 路径和用户端路由；
- OpenAI 模型/对话/Embedding 已统一为 `/v1/*`；平台用户、Key、充值、通知、2FA 已按当前路由更新；
- 余额流水和调用记录仍标记为待路由核验，未将不确定路径写成正式契约；
- `api-reference.md` 当前为 `review`、`v1.0.0`；
- 文档修订不代表 API 运行态或契约测试通过。

## 12. 角色与权限文档同步记录（23:55）

- `docs/data-dictionary.md`、`SPEC-§30-权限管理.md`、`ref-2.1-roles-permissions.md`、权限架构章节和 glossary 已同步 ADR-0024。
- 当前 canonical 角色和 grant/deny 优先级已统一；未实现权限覆写接口标记为 planned。
- 实际路由中的 `finance` 运行时角色样本保留为 DOC_CODE_GAP，未修改源码。
- 本次文档同步不代表权限实现和测试已通过。

## 13. 迁移、备份与发布门禁同步记录（00:10）

- 部署清单与 Runbook 已统一引用唯一 `release-baseline.md`，不再把 1159/1132/808 作为正式基线。
- 手写迁移入口统一为 `api/` 下 `pnpm run db:migrate:manual`；custom dump 回滚统一为 `pg_restore`。
- Runbook 已明确 `.env` 配置和连通性检查先于 `deploy.sh`；`deploy.sh`、真实备份任务、恢复演练和发布基线仍待证据核验。
- 迁移 SPEC 已取消固定批大小和未经验证自动回滚表述。

## 14. 未决事项处理记录（23:17）

- `open-issues.md` 第 1 项已由 BOSS 确认解决。
- 新增 `ADR-0020-refund-direction-and-negative-balance.md`，状态 `accepted`，版本 `v1.0.0`。
- 已同步 ADR-0003、退款 PRD、退款 SPEC、退款状态机和决策登记。
- `open-issues.md` 已将第 1 项标记为已解决，并升至 `v0.2.0`。
- 本项不再作为未决阻塞；剩余第 2–10 项保持未决。
