# 用户体系需求交叉核对结果

复核日期：2026-08-30

## 范围

- 当前源文档：`docs/PRD-用户体系.md`
- 辅助对照文档：`docs/SPEC-§2-用户体系.md`
- 受影响需求：407 条
- 交叉核对明细：`user-system-crosscheck.csv`

## 结果

- 找到语义相似辅助参考的记录：21 条。
- 其余 386 条：未找到达到最低相似度的可读 SPEC 参考，继续人工复核。
- 21 条仅是人工复核候选，**不是自动恢复结果**。
- 当前 `source_path` 仍保持为 PRD，不得把 SPEC 直接写成需求出处。

## 人工复核要求

对 21 条候选逐条确认：

1. 当前 PRD 的章节是否能通过可读标题或明确行号定位；
2. SPEC 的描述是否只是深化说明，而不是对 PRD 的替代；
3. `source_quote` 是否必须回到 PRD 当前文件重新摘录；
4. `atomic_assertion` 是否只包含一个可验证条件、动作或结果；
5. 是否需要新增 `derived_from` 关系来表示由一条原始需求拆出的多个断言。

## 当前结论

本批次仍未达到自动修复门槛。不得因为存在 SPEC 相似文本而清除编码问题，也不得把候选状态提升为 PASS。正式需求验收继续 BLOCKED。
