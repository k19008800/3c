# 登录模块 — 测试用例

**总用例数：9**

---

## 1. 登录模块

### 1.1 正常登录 — 成功流程

| 编号 | TC-LOGIN-001 |
|------|-------------|
| **前置条件** | 存在已注册邮箱 `test@example.com`，密码正确 |
| **测试步骤** | ① 访问 `/login` → ② 输入邮箱、密码 → ③ 点击「登录」按钮 |
| **预期结果** | ① 登陆按钮显示 loading 旋转动画 → ② 跳转到 `/console`（仪表盘）→ ③ 侧边栏显示用户邮箱、余额、角色 → ④ 首页显示"欢迎回来！"横幅 |
| **级别** | L1 |
| **状态** | □ 待测试 □ 通过 □ 失败 |

### 1.2 密码错误

| 编号 | TC-LOGIN-002 |
|------|-------------|
| **前置条件** | 存在账号 `test@example.com`，输入错误密码 |
| **测试步骤** | ① 输入正确邮箱 + 随机错误密码 → ② 点击「登录」 |
| **预期结果** | ① 错误提示框出现（AlertCircle 图标 + 文案）→ ② 提示文案包含"请检查邮箱和密码"或服务端返回的message → ③ 停留在 `/login` 页面不跳转 → ④ 密码输入框内容保留 |
| **级别** | L1 |
| **状态** | □ 待测试 □ 通过 □ 失败 |

### 1.3 空输入提交

| 编号 | TC-LOGIN-003 |
|------|-------------|
| **前置条件** | 无 |
| **测试步骤** | ① 邮箱/密码均留空 → ② 点击「登录」 |
| **预期结果** | ① HTML5 required 校验阻止提交（显示浏览器默认"请填写此字段"气泡）→ ② 即使绕过 required，服务端返回 400 错误提示 |
| **级别** | L2 |
| **状态** | □ 待测试 □ 通过 □ 失败 |

### 1.4 不存在账号

| 编号 | TC-LOGIN-004 |
|------|-------------|
| **前置条件** | 输入不存在的邮箱 `nonexist@test.com` |
| **测试步骤** | ① 输入不存在邮箱 + 任意密码 → ② 点击登录 |
| **预期结果** | ① 错误提示框出现 → ② 文案不应泄露"账号不存在"细节（应为统一错误提示）→ ③ 不跳转 |
| **级别** | L2 |
| **状态** | □ 待测试 □ 通过 □ 失败 |

### 1.5 验证码弹窗（风控触发）

| 编号 | TC-LOGIN-005 |
|------|-------------|
| **前置条件** | 多次失败登录触发风控，或服务端返回 `captchaRequired` |
| **测试步骤** | ① 输入邮箱密码 → ② 点击登录 → ③ 服务端返回 captchaRequired → ④ CaptchaDialog 弹出 |
| **预期结果** | ① 弹窗显示标题"输入邮箱验证码"、邮箱地址、验证码输入框 → ② 错误显示"检测到异常登录，请输入邮箱验证码" → ③ 提交验证码 → ④ 验证成功→登录→跳转；验证失败→弹窗保留，显示"验证码错误" |
| **级别** | L1 |
| **状态** | □ 待测试 □ 通过 □ 失败 |

### 1.6 登录后自动重定向

| 编号 | TC-LOGIN-006 |
|------|-------------|
| **前置条件** | 已登录状态（accessToken 有效） |
| **测试步骤** | ① 访问 `/login` |
| **预期结果** | ① 自动跳转到 `/console`，不显示登录表单 |
| **级别** | L2 |
| **状态** | □ 待测试 □ 通过 □ 失败 |

### 1.7 登录表单 UI 样式校验

| 编号 | TC-LOGIN-007 |
|------|-------------|
| **前置条件** | 未登录 |
| **测试步骤** | ① 检查登录页整体渲染 |
| **预期结果** | ① 背景为 `slate-900 → slate-800` 渐变 → ② 白色卡片居中，宽度 `max-w-md` → ③ 标题"3Cloud"三号粗体 → ④ 描述"AI Token 聚合平台" → ⑤ 邮箱输入框带 label、placeholder → ⑥ 密码输入框 type=password、带"忘记密码？"链接 → ⑦ 登录按钮蓝色 `bg-blue-600` → ⑧ "还没有账号？立即注册"链接 → ⑨ 所有输入框 focus 时显示 ring-2 ring-blue-500 |
| **级别** | L2 |
| **状态** | □ 待测试 □ 通过 □ 失败 |

### 1.8 登录按钮 Loading 状态

| 编号 | TC-LOGIN-008 |
|------|-------------|
| **前置条件** | 输入合法凭证 |
| **测试步骤** | ① 点击登录 → ② 观察按钮状态 |
| **预期结果** | ① 按钮 disabled + opacity-50 → ② 按钮内出现 Loader2 旋转图标 → ③ 文案"登录"保留 → ④ 不可重复点击 |
| **级别** | L2 |
| **状态** | □ 待测试 □ 通过 □ 失败 |

### 1.9 错误弹窗关闭/消失

| 编号 | TC-LOGIN-009 |
|------|-------------|
| **前置条件** | 密码错误触发错误提示 |
| **测试步骤** | ① 错误显示后 → ② 修正输入 → ③ 重新提交 |
| **预期结果** | ① 新提交时 `setError('')` 清除旧错误 → ② 旧错误闪烁后消失 → ③ 新错误/成功正常展示 |
| **级别** | L3 |
| **状态** | □ 待测试 □ 通过 □ 失败 |
