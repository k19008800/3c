# 用户控制台 — 个人设置 — 测试用例

**总用例数：7**

---

## 15. 用户控制台 — 个人设置

### 15.1 Tab 导航

| 编号 | TC-SETTINGS-001 |
|------|--------------|
| **测试步骤** | ① 访问 `/console/settings` |
| **预期结果** | ① 5 个 Tab：个人资料/安全/会话管理/登录历史/通知偏好 → ② 默认选中"个人资料" → ③ Tab 切换内容正确 |
| **级别** | L1 |

### 15.2 个人资料 — 昵称编辑

| 编号 | TC-SETTINGS-002 |
|------|--------------|
| **测试步骤** | ① 修改昵称 → ② 点击「保存」 |
| **预期结果** | ① 保存成功：绿色提示"昵称更新成功" → ② 3秒提示消失 → ③ localStorage 中的 user 同步更新 |
| **级别** | L1 |

### 15.3 个人资料 — 邮箱验证

| 编号 | TC-SETTINGS-003 |
|------|--------------|
| **前置条件** | 邮箱未验证 |
| **测试步骤** | ① 点击「发送验证码」→ ② 输入验证码 → ③ 点击「验证」 |
| **预期结果** | ① 琥珀色提示框显示"邮箱未验证" → ② 发送按钮 loading 态 → ③ 验证成功后页面 reload → ④ 邮箱状态变为"已验证"+绿色徽章 |
| **级别** | L1 |

### 15.4 安全 — 修改密码

| 编号 | TC-SETTINGS-004 |
|------|--------------|
| **测试步骤** | ① 输入旧密码/新密码(≥6位)/确认密码 → ② 点击「修改密码」 |
| **预期结果** | ① 前置校验：旧密码不能为空/新密码≥6位/两次一致 → ② 提交成功后清空表单 → ③ 绿色提示"密码修改成功" |
| **级别** | L1 |

### 15.5 密码显示/隐藏切换

| 编号 | TC-SETTINGS-005 |
|------|--------------|
| **测试步骤** | ① 点击旧密码/新密码输入框的 Eye/EyeOff 图标 |
| **预期结果** | ① type 在 password ↔ text 切换 → ② 图标同步切换 |
| **级别** | L2 |

### 15.6 通知偏好

| 编号 | TC-SETTINGS-006 |
|------|--------------|
| **测试步骤** | ① 切换通知开关 → ② 点击「保存偏好」 |
| **预期结果** | ① 3 个 Switch 开关（充值成功通知/余额不足提醒/每日用量汇总）→ ② 余额提醒开启时显示阈值输入框（带¥前缀）→ ③ 阈值支持小数输入 → ④ 有变更时按钮 active → ⑤ 保存成功提示 |
| **级别** | L1 |

### 15.7 会话管理

| 编号 | TC-SETTINGS-007 |
|------|--------------|
| **测试步骤** | ① 切换到"会话管理"Tab |
| **预期结果** | ① 与 Security 页面会话管理功能一致 → ② 支持单个下线/全部下线 |
| **级别** | L1 |
