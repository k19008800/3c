# 用户控制台 — 仪表盘 — 测试用例

**总用例数：7**

---

## 5. 用户控制台 — 仪表盘

### 5.1 仪表盘完整渲染

| 编号 | TC-DASH-001 |
|------|------------|
| **测试步骤** | ① 登录后自动跳转到 `/console` |
| **预期结果** | ① 欢迎横幅（渐变蓝紫背景 + 邮箱 + 余额 + 角色）→ ② 统计周期切换按钮（今日/本周/本月）→ ③ StatCard × 4（调用次数、Token消耗、消费、余额），每张带对应颜色图标 → ④ 额度使用情况（QuotaWidget，带进度条和告警）→ ⑤ 用量总览（可折叠展开）→ ⑥ 快捷操作 × 4（充值/API密钥/调用日志/复制curl）→ ⑦ 最近登录 × 3 |
| **级别** | L1 |

### 5.2 统计周期切换

| 编号 | TC-DASH-002 |
|------|------------|
| **测试步骤** | ① 点击"本周" → ② 点击"本月" → ③ 点击"今日" |
| **预期结果** | ① 按钮 active 态切换（白色背景 + shadow）→ ② StatCard 数据刷新 → ③ 用量总览内数据对应周期更新 |
| **级别** | L1 |

### 5.3 用量总览展开/折叠

| 编号 | TC-DASH-003 |
|------|------------|
| **测试步骤** | ① 点击"用量总览"标题区域 |
| **预期结果** | ① 展开详情区域（渐变色背景）→ ② Tab 切换栏出现：概览/趋势/模型/Key对比 → ③ 默认选中"概览"Tab → ④ 再次点击头部 → 折叠 |
| **级别** | L1 |

### 5.4 用量 Tab 切换

| 编号 | TC-DASH-004 |
|------|------------|
| **测试步骤** | ① 展开用量总览 → ② 依次点击概览/趋势/模型/Key对比 |
| **预期结果** | ① 每个 Tab 内容独立加载 → ② 趋势 Tab 显示 Token 消耗柱状图 + 每日热力分布 → ③ 模型 Tab 显示模型排行表 → ④ Key对比 Tab 显示各密钥数据 → ⑤ 数据无交叉污染 |
| **级别** | L1 |

### 5.5 快捷操作导航

| 编号 | TC-DASH-005 |
|------|------------|
| **测试步骤** | ① 点击"充值"卡片 → ② 点击"API 密钥" → ③ 点击"调用日志" → ④ 点击"复制 curl" |
| **预期结果** | ①→③ 分别跳转到对应页面 → ④ 复制 cURL 到剪贴板，显示"已复制！"反馈 |
| **级别** | L1 |

### 5.6 额度告警

| 编号 | TC-DASH-006 |
|------|------------|
| **前置条件** | 额度使用率 ≥ alertPercent (默认80%) |
| **预期结果** | ① 进度条变为琥珀色/红色 → ② 出现 AlertCircle + "额度即将用完"提示 + "去充值 →"链接 → ③ 链接跳转到 `/recharge` |
| **级别** | L2 |

### 5.7 Loading/Error 态

| 编号 | TC-DASH-007 |
|------|------------|
| **测试步骤** | ① 观察数据加载过程 → ② 模拟网络异常 |
| **预期结果** | ① 加载中：Loader2 居中旋转 → ② 加载失败：红色 AlertCircle + 错误文案 |
| **级别** | L3 |
