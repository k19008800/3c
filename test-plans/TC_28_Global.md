# 全局交互规则校验 — 测试用例

**总用例数：7**

---

## 28. 全局交互规则校验

### 28.1 无权限页面访问

| 编号 | TC-GLOBAL-001 |
|------|-------------|
| **前置条件** | 普通用户角色 |
| **测试步骤** | ① 直接 URL 访问 `/console/admin/users` |
| **预期结果** | ① AdminRoute 拦截 → ② 显示"无权限访问"提示页或重定向 |
| **级别** | L2 |

### 28.2 Token 过期

| 编号 | TC-GLOBAL-002 |
|------|-------------|
| **前置条件** | accessToken 已过期 |
| **测试步骤** | ① 刷新页面或发送请求 |
| **预期结果** | ① API 返回 401 → ② 自动清除 token → ③ 跳转到 `/login` → ④ 显示"登录已过期，请重新登录"提示 |
| **级别** | L2 |

### 28.3 未登录访问控制台

| 编号 | TC-GLOBAL-003 |
|------|-------------|
| **前置条件** | 未登录 |
| **测试步骤** | ① 访问 `/console` 任何路径 |
| **预期结果** | ① Navigate to="/login" → 自动跳转到登录页 |
| **级别** | L2 |

### 28.4 分页组件通用校验

| 编号 | TC-GLOBAL-004 |
|------|-------------|
| **测试步骤** | ① 查看任意带分页的页面 |
| **预期结果** | ① PaginationBar 正确显示总页数/当前页 → ② 上一页/下一页/页码按钮可用 → ③ 页码 1 时"上一页"disabled → ④ 最后一页时"下一页"disabled → ⑤ 当前页高亮 → ⑥ 点击页码触发数据刷新 |
| **级别** | L1 |

### 28.5 错误边界

| 编号 | TC-GLOBAL-005 |
|------|-------------|
| **测试步骤** | ① 模拟组件渲染异常 |
| **预期结果** | ① ErrorBoundary 捕获异常 → ② 显示友好错误提示而不是白屏 → ③ 含"重新加载"按钮 |
| **级别** | L3 |

### 28.6 浏览器后退/前进

| 编号 | TC-GLOBAL-006 |
|------|-------------|
| **测试步骤** | ① 在多个页面间导航 → ② 浏览器后退 → ③ 浏览器前进 |
| **预期结果** | ① 后退到上一个历史页面 → ② 页面状态正确 → ③ 不出现参数丢失/空白 |
| **级别** | L2 |

### 28.7 页面刷新后状态保持

| 编号 | TC-GLOBAL-007 |
|------|-------------|
| **测试步骤** | ① 在任意页面 → ② 按 F5 刷新 |
| **预期结果** | ① 保持登录状态 → ② 保持在当前路由 → ③ 数据重新加载 |
| **级别** | L2 |
