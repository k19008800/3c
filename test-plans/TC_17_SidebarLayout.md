# 控制台通用 — 侧边栏/布局 — 测试用例

**总用例数：8**

---

## 17. 控制台通用 — 侧边栏/布局

### 17.1 侧边栏渲染

| 编号 | TC-SIDEBAR-001 |
|------|--------------|
| **测试步骤** | ① 登录后观察侧边栏 |
| **预期结果** | ① 深色背景 `bg-slate-900` → ② 顶部 logo + 折叠按钮 → ③ 导航分组（总览/资源与服务/数据与分析/财务与消费/账户/消息）→ ④ 管理员可见"⚙️ 管理"区域（分组可折叠）→ ⑤ 底部用户信息 + 通知铃铛 + 退出按钮 |
| **级别** | L1 |

### 17.2 侧边栏折叠

| 编号 | TC-SIDEBAR-002 |
|------|--------------|
| **测试步骤** | ① 点击顶部折叠按钮 (ChevronLeft) |
| **预期结果** | ① 宽度从 w-60 → w-16 → ② 文字隐藏 → ③ 图标居中 → ④ hover 显示 title → ⑤ 再次点击展开 (ChevronRight) → ⑥ 状态持久化（localStorage？检查） |
| **级别** | L1 |

### 17.3 管理分组折叠

| 编号 | TC-SIDEBAR-003 |
|------|--------------|
| **前置条件** | 管理员角色 |
| **测试步骤** | ① 点击管理区域的组标题（如"💰 财务结算"） |
| **预期结果** | ① 该组折叠/展开 → ② ChevronDown 旋转动画 → ③ 状态只影响点击组 |
| **级别** | L1 |

### 17.4 通知下拉

| 编号 | TC-SIDEBAR-004 |
|------|--------------|
| **测试步骤** | ① 点击底部铃铛图标 |
| **预期结果** | ① 弹出下拉列表 → ② 显示最近 5 条通知 → ③ 未读显示蓝色背景 + 加粗 + 「标为已读」按钮 → ④ 点击"查看全部"跳转到通知中心 → ⑤ 外框点击关闭 → ⑥ 未读数红色徽章 |
| **级别** | L1 |

### 17.5 退出登录

| 编号 | TC-SIDEBAR-005 |
|------|--------------|
| **测试步骤** | ① 点击底部「退出登录」 |
| **预期结果** | ① 清除 localStorage (accessToken/refreshToken/user) → ② 跳转到 `/login` → ③ 访问 `/console` 重定向到 `/login` |
| **级别** | L1 |

### 17.6 移动端侧边栏

| 编号 | TC-SIDEBAR-006 |
|------|--------------|
| **测试步骤** | ① 缩小浏览器窗口至 <1024px |
| **预期结果** | ① 侧边栏隐藏 → ② 左上角汉堡菜单按钮出现 → ③ 点击打开侧边栏遮罩层 → ④ 点击遮罩层关闭 → ⑤ 再次点击汉堡按钮关闭 |
| **级别** | L2 |

### 17.7 模拟模式横幅

| 编号 | TC-SIDEBAR-007 |
|------|--------------|
| **前置条件** | 管理员模拟(impersonate)用户 |
| **预期结果** | ① 页面顶部固定琥珀色横幅 → ② 显示"模拟模式"标签 + 目标用户邮箱 → ③ 复制Token按钮 → ④ 倒计时（5秒刷新）→ ⑤ 「退出模拟」按钮 →  释放后横幅消失 |
| **级别** | L1 |

### 17.8 Loading 态

| 编号 | TC-SIDEBAR-008 |
|------|--------------|
| **前置条件** | accessToken 存在但 /auth/me 请求未返回 |
| **预期结果** | ① AppLayout 显示 Loader2 全屏居中旋转 → ② 请求返回后显示正常布局 → ③ 401 时跳转 `/login` |
| **级别** | L2 |
