# 用户控制台 — API 密钥 — 测试用例

**总用例数：9**

---

## 7. 用户控制台 — API 密钥

### 7.1 密钥列表渲染

| 编号 | TC-AK-001 |
|------|----------|
| **测试步骤** | ① 访问 `/console/api-keys` |
| **预期结果** | ① 标题"API 密钥" + 密钥计数徽章 → ② 表格：名称/密钥前缀/状态标签/最后使用/创建时间/操作列 → ③ "创建密钥"按钮 |
| **级别** | L1 |

### 7.2 创建密钥弹窗

| 编号 | TC-AK-002 |
|------|----------|
| **测试步骤** | ① 点击「创建密钥」→ ② 弹窗出现 → ③ 输入名称 → ④ 点击「确认创建」 |
| **预期结果** | ① 遮罩层 + 白色弹窗居中 → ② 输入框 autofocus → ③ 创建成功关闭弹窗 → ④ 绿色成功条显示完整密钥 + 「复制」「关闭」按钮 → ⑤ 列表刷新含新密钥 |
| **级别** | L1 |

### 7.3 密钥复制

| 编号 | TC-AK-003 |
|------|----------|
| **测试步骤** | ① 成功创建后点击「复制」 |
| **预期结果** | ① 复制到剪贴板 → ② 按钮文字变为"已复制" + CheckCircle2 → ③ 3秒后恢复 |
| **级别** | L1 |

### 7.4 密钥启用/停用

| 编号 | TC-AK-004 |
|------|----------|
| **测试步骤** | ① 点击操作列 PowerOff/Power 图标 |
| **预期结果** | ① 状态标签即时切换（绿色"启用"↔红色"停用"）→ ② 图标切换（Power/PowerOff）→ ③ 按钮颜色变化 |
| **级别** | L1 |

### 7.5 密钥删除

| 编号 | TC-AK-005 |
|------|----------|
| **测试步骤** | ① 点击「删除」 |
| **预期结果** | ① 弹出 `window.confirm` 确认框（"确定要删除此 API 密钥吗？此操作不可撤销。"）→ ② 确认 → 列表移除该行 + 计数减1 → ③ 取消 → 不做任何操作 |
| **级别** | L1 |

### 7.6 用量面板展开

| 编号 | TC-AK-006 |
|------|----------|
| **测试步骤** | ① 点击行首 ChevronRight 图标 → ② 展开用量面板 |
| **预期结果** | ① 行展开展示 KeyUsageDashboard → ② 4 个 Tab：概览/模型分布/趋势/Key对比 → ③ 概览 Tab 显示今日/本月/累计数据卡片 + 成功率条 → ④ 3 个导出按钮（今日/本月/全部）→ ⑤ 再次点击 → 折叠 |
| **级别** | L1 |

### 7.7 用量 Tab 数据加载

| 编号 | TC-AK-007 |
|------|----------|
| **测试步骤** | ① 展开用量 → ② 点击各个 Tab |
| **预期结果** | ① 模型分布 Tab：展示模型调用排行表 → ② 趋势 Tab：7天柱状图 + 24小时热力图 → ③ Key对比 Tab：各密钥数据对比表 → ④ 数据加载正确无空白 |
| **级别** | L2 |

### 7.8 空列表态

| 编号 | TC-AK-008 |
|------|----------|
| **前置条件** | 无任何 API Key |
| **预期结果** | ① 显示 Key 图标 + "暂无 API 密钥" → ② "创建第一个密钥"可点击链接 |
| **级别** | L2 |

### 7.9 创建密钥弹窗验证

| 编号 | TC-AK-009 |
|------|----------|
| **测试步骤** | ① 弹窗打开 → ② 名称留空 → ③ 点击确认 |
| **预期结果** | ① 按钮 disabled（`!newKeyName.trim()`）→ ② 无法提交 |
| **级别** | L2 |
