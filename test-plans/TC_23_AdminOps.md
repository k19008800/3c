# 管理后台 — 运维配置 — 测试用例

**总用例数：4**

---

## 23. 管理后台 — 运维配置

### 23.1 系统配置

| 编号 | TC-ADMIN-CONFIG-001 |
|------|-------------------|
| **测试步骤** | ① 访问 `/console/admin/configs` |
| **预期结果** | ① 配置项列表（key/value/描述）→ ② 编辑配置 → ③ 保存生效 |
| **级别** | L1 |

### 23.2 限流管理

| 编号 | TC-ADMIN-RATELIMIT-001 |
|------|----------------------|
| **测试步骤** | ① 访问 `/console/admin/rate-limits` |
| **预期结果** | ① 限流规则列表 → ② 搜索 → ③ 创建规则（RPM/TPM/有效期）→ ④ 编辑/删除 → ⑤ 水位显示 → ⑥ 时间范围筛选 → ⑦ 限流状态写 call_logs |
| **级别** | L1 |

### 23.3 邮件模板

| 编号 | TC-ADMIN-EMAIL-001 |
|------|------------------|
| **测试步骤** | ① 访问 `/console/admin/email-templates` |
| **预期结果** | ① 模板列表 → ② 编辑内容/标题 → ③ 预览 → ④ 保存 |
| **级别** | L1 |

### 23.4 内容管理

| 编号 | TC-ADMIN-PAGECONTENT-001 |
|------|------------------------|
| **测试步骤** | ① 访问 `/console/admin/page-contents` |
| **预期结果** | ① 门户页面内容管理 → ② 编辑门户首页/定价页内容 → ③ 保存并上线 |
| **级别** | L1 |
