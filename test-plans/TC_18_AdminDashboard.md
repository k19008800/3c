# 管理后台 — 仪表盘 — 测试用例

**总用例数：2**

---

## 18. 管理后台 — 仪表盘

### 18.1 管理仪表盘渲染

| 编号 | TC-ADMIN-DASH-001 |
|------|-----------------|
| **测试步骤** | ① 访问 `/console/admin` |
| **预期结果** | ① 时间预设选择器（30分钟/1h/3h/6h/12h/24h/7天/30天/自定义）→ ② AlertBar（告警横幅）→ ③ KpiCards（KPI指标卡）→ ④ RevenueBreakdown（营收拆解图）→ ⑤ TopUsersTable（消费Top用户表）→ ⑥ ModelRankBar（模型排行柱状图）→ ⑦ VendorHealthPanel（供应商健康面板）→ ⑧ ModelSchedulingRealtime（模型调度实时看板）→ ⑨ TodoQueuePanel（待办队列） |
| **级别** | L1 |

### 18.2 时间预设切换

| 编号 | TC-ADMIN-DASH-002 |
|------|-----------------|
| **测试步骤** | ① 依次点击不同时间预设 |
| **预期结果** | ① 当前预设高亮 → ② 所有数据卡片/图表对应刷新 → ③ "自定义"弹出时间选择器 |
| **级别** | L1 |
