# pnpm 配置
# hoisted: 扁平 node_modules（Windows 更稳定，避免 symlink 卡顿）；后续如需严格隔离可改回 isolated
node-linker=hoisted
# 关闭无关提示
strict-peer-dependencies=false
# 使用国内镜像（npmjs 直连 ECONNRESET 不稳定）
registry=https://registry.npmmirror.com
