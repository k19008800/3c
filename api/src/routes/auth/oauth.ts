// ============================================================
//  3cloud (3C) — OAuth 路由（预留）
//  当前未实现第三方登录，此处为占位入口
// ============================================================

import { FastifyInstance } from "fastify";

export async function authOAuthRoutes(_app: FastifyInstance) {
  // OAuth 路由预留 — 未来接入 GitHub / Google / WeChat 等第三方登录
}
