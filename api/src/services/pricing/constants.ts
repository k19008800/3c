// ============================================================
//  3cloud (3C) — 定价常量定义
// ============================================================

// ── 全局定价倍率默认值 ──
// 1.15 = 15% 加价。所有模块必须引用此常量，确保默认值一致。
export const DEFAULT_PRICING_MULTIPLIER = 1.15;