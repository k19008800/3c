// ============================================================
//  3cloud (3C) — 限流中间件
//  4 级 × 2 维度 (RPM/TPM) — Redis 滑动窗口
//  占位 — 后续开发实现
// ============================================================

/**
 * 限流级别：
 * 1. Key 级 — 单个 API Key
 * 2. 用户级 — 单个用户
 * 3. 用户类型默认 — 个人 / 企业分开
 * 4. 全局兜底
 *
 * 维度：RPM（请求/分钟）/ TPM（Token/分钟）
 *
 * 默认阈值：
 * - 个人：RPM 60 / TPM 100,000
 * - 企业：RPM 300 / TPM 500,000
 * - 全局：RPM 30 / TPM 50,000
 * - 单用户覆盖：后台可设
 */
export async function checkRateLimit(
  userId: number,
  apiKeyId: number,
  userType: string
) {
  // TODO: 使用 Redis 滑动窗口实现限流
  // 1. 依次检查 Key 级 → 用户级 → 类型默认 → 全局
  // 2. 任意一级超限即返回 429 + Retry-After
}
