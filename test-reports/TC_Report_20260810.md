# 3cloud 测试执行报告 — 2026-08-10

> 范围：单元测试 + 集成链路测试 + E2E，并核对留存测试数据。
> 架构基线：单前端 + 单后端（api@3000 / web-portal@5177 静态托管 web-console 于 `/app/`），库 `threecloud_v3`。

## 1. 执行结果汇总

| 层 | 命令 | 结果 |
|---|---|---|
| 单元测试 | `pnpm test`（vitest） | ✅ **114/114 通过**（11 个文件） |
| 集成链路 | `node scripts/test-integration.cjs` | ✅ **17/17 通过** |
| E2E | `cd e2e && npx playwright test` | ✅ **5/5 通过**（msedge，26.3s） |

### 1.1 单元测试明细（11 文件 / 114 用例）

`api/test/`：auth(8)、billing(7)、body-preprocessor、circuit-breaker(9)、health(2)、key-selector(10)、pipeline、routing(12)、sse-stream(9)、suppliers(32)、token-count(14)。

### 1.2 集成链路 17 项（`scripts/test-integration.cjs`）

Health → 注册(¥10 赠金) → 登录 → `/me`(balance=¥10) → 建 Key(`3c_…`) → 列表 → 禁用/启用 → chat(mock, 72 tokens) → 日志 → 余额扣减(¥10→¥9.9995) → 今日统计 → 账单(current) → Web(5177/app=200) → OpenAI via portal /v1/models(5) → Public Pricing(12)。

### 1.3 E2E（`e2e/tests/console.spec.ts`，5 项）

注册 → 登录 → 仪表盘(余额) / 创建 API Key(`3c_` 明文) / Key 禁用-启用 / 调用日志空态 / 未登录落 Portal 入口。

## 2. 本次修复/对齐

E2E 原配置指向**已停用的 5175** 且凭证/前缀过时，本次对齐单入口架构：

- `e2e/playwright.config.ts`：baseURL `5175` → `http://localhost:5177/app`。
- `e2e/tests/console.spec.ts`：凭证 `admin@3cloud.io/seed-admin` → 注册-登录真实链路；Key 前缀 `sk-` → `3c_`；标题 `仪表盘`/`API Key 管理`；路由含 `/app` basename；断言对齐真实 UI（余额 `.first()`、日志空态、shared-ui Table 无 `columnheader` role）。

## 3. 留存测试数据（threecloud_v3，未清除）

### 3.1 表级计数（截至执行后）

| 表 | 行数 |
|---|---|
| users | 94 |
| api_keys | 28 |
| consumption_records | 13 |
| balance_transactions | 50 |
| recharge_orders | 7 |
| suppliers | 13 |
| supplier_models | 12 |
| user_sessions | 204 |
| audit_logs / tickets | 0 |

### 3.2 本次运行留存明细

**集成测试**（15:16，`integ-1786346176974@test.com`）：

- 注册赠金：`balance_transactions` `adjustment +10 → 10.0000`
- chat 记账：`consumption_records id=15`，`deepseek-chat`，11+61=72 tokens，`cost=0.00051`
- 扣费：`balance_transactions` `consumption -0.0005 → 9.9995`
- API Key：`Test`（active）
- 账单 API：`/me/billing/current` → `{"period":"2026-08","total_cost":0.00051,"bill_count":1,"days_left":20}`

**E2E**（15:24–15:26，每轮随机邮箱 `{reg,key,op,logs}-<ts>@e2e.test`）：

- 注册 9 名 `customer`，各持 `available_balance=10.0000`（赠金）
- `api_keys` 新增 6 枚 `e2e-key-*` / `e2e-op-*`（active）
- 调用日志页无记录（消费记录表中 `@e2e.test` 无行）

> 说明：新注册用户余额 10.0000、无消费记录属正常（E2E 未调 chat）；有调用数据的留存体现在集成测试用户。

## 4. 遗留观察（非本次缺陷）

1. **E2E 启用/禁用断言简化**：禁用后再点启用，行内「启用」徽标与按钮同名，断言取首即可；已通过。
2. **新用户日志页无表头**：`LogsPage` 空态分支不渲染表头 `<th>`（Table 空态）；集成测试已覆盖 `/me/logs` 表结构，前端可后续考虑空态下仍显示列头。
3. **未登录访问 `/app/`**：落在 Portal 官网首页（http://localhost:5177/）而非业务登录页；属 portal 静态托管路由设计，非缺陷。
