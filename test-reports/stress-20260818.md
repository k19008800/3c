# 3cloud 压测报告 — 并发计费一致性（P3-1）

> **日期**：2026-08-18
> **场景**：同用户 10 并发 × 2 轮 = 20 次真实 chat 调用（mock 回退路径，上游供应商均 offline）
> **模型**：deepseek-v4-pro（~150K tokens/笔 ≈ ¥0.45~0.75/笔）
> **前置**：分区改造已完成（migration 0025，consumption_records / balance_transactions 为按月 RANGE 分区表）

## 压测命令

```bash
node scripts/stress-chat.cjs 10 2
```

## 结果汇总

| 断言 | 结果 |
|------|------|
| 全部请求成功 | ✅ 20/20 ok（109.7s，0.2 rps——单笔 150K tokens 大上下文，mock 回退生成 45 万字符响应） |
| 今日调用数 == 请求数 | ✅ todayCallCount=20 |
| 今日消费 > 0 | ✅ ¥1.4986 |
| 余额扣减 == 消费 | ✅ ¥50 → ¥48.50138（扣 ¥1.4986） |
| consumption_records 条数 == 请求数 | ✅ cnt=20 |
| request_id 无重复 | ✅ count(distinct request_id)=20 |
| sum(cost) == 余额扣减 | ✅ sum(cost)=1.49862000 |
| balance_transactions 消费笔数 == 请求数 | ✅ consumption 笔数=20 |
| 无冻结残留 | ✅ frozen_balance=0（预扣路径：余额 ¥50 ≤ ¥100 阈值 → 预扣冻结 → 结算解冻，多退少补后归零） |

**🎯 9/9 passed**

## 分区表下行为验证（本次压测附带）

- `consumption_records` / `balance_transactions` 均为分区表（relkind=p），16 个子表（2026_05 ~ 2026_11 + default）
- 数据全部落在 `consumption_records_2026_08`（3161 行）/ `balance_transactions_2026_08`（6573 行），default 与未来子表为空
- 分区裁剪：按 created_at 的查询命中正确子表
- 幂等 L2 DB 兜底：`isIdempotencyUniqueViolation` 正则已适配复合唯一约束名（父表 `consumption_records_request_id_created_at_unique` 与子表索引 `consumption_records_2026_08_request_id_created_at_key`），单测覆盖两种形态

## 大表索引审查（migration 0025）

| 表 | 索引 | 说明 |
|----|------|------|
| consumption_records | pkey (id, created_at) | 分区表复合主键（含分区列） |
| consumption_records | request_id 复合唯一 (request_id, created_at) | 幂等 L2 兜底（分区表唯一约束必须含分区列） |
| consumption_records | idx_consumption_records_user_created (user_id, created_at) | /me/stats、/me/logs、admin 列表高频查询 |
| balance_transactions | pkey (id, created_at) | 分区表复合主键 |
| balance_transactions | idx_balance_transactions_user_created (user_id, created_at) | 资金流水按用户查询 |
| balance_transactions | idx_balance_transactions_type_created (type, created_at) | 退款统计 / 月结 |

## 结论

并发计费一致性通过：条数、金额、余额、幂等四维度精确对齐，无超扣/漏账/重复记账；分区表落地后行为与普通表一致（幂等兜底、预扣解冻均正常）。**P3-1 压测项通过。**

## 备注

- 压测账号留存：`stress-1787054968552@test.com`（余额 ¥48.50）
- 本次为 mock 回退路径（无真实上游供应商 active）；真实上游压测待供应商配置后补充（计费路径一致，仅上游耗时不同）
- vitest 全量回归：见 P3 验收 Gate（基线 792 + P2/P3 新增）
