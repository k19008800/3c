# 3cloud 数据库分析报告

**分析时间**: 2026-07-23 21:47 GMT+8
**数据库**: threecloud
**PostgreSQL版本**: 17.10

## 1. 数据库概览

- **总表数量**: 83 张表
- **总行数**: 约 2,232,959 行
- **数据库大小**: 1,221 MB (约 1.2GB)
- **最大表**: call_logs_202607 (875 MB)
- **第二大表**: balance_logs (267 MB)

## 2. 表大小TOP 10

| 表名 | 大小 | 行数 |
|------|------|------|
| call_logs_202607 | 875 MB | 17,764,445 |
| balance_logs | 267 MB | 304,284 |
| call_logs_202606 | 26 MB | 652,242 |
| commission_logs_202606 | 20 MB | 382,733 |
| commission_logs_2026_05 | 8,280 kB | 27,355 |
| user_notifications | 1,808 kB | 7,919 |
| commission_logs_202607 | 1,736 kB | 21,283 |
| user_login_sessions | 1,392 kB | 1,014 |
| operation_logs | 1,104 kB | 2,028 |
| audit_logs | 976 kB | 1,894 |

## 3. 索引分析

### 未使用索引
通过查询 `pg_stat_user_indexes` 中 `idx_scan = 0` 的索引：
- **call_logs 分区表**有大量未使用的索引（52个）
- **commission_logs 分区表**也有大量未使用的索引（8个）

**主要问题**：
1. 分区表的索引未被有效利用
2. 部分业务表的索引扫描次数为0

### 索引命中率
- 用户表、API密钥表等核心表的索引使用频繁
- 日志表的索引使用率较低

## 4. 外键约束分析

查询显示存在多个缺少外键约束的关联字段：

**缺少外键的表字段**：
- 多个以 `_id` 结尾的字段缺少外键约束
- 建议为这些关联字段添加外键以保证数据完整性

## 5. 分区表分析

检测到以下分区表：

### 主分区表 → 分区
- **commission_logs** (主表) → 各月份分区表
- **call_logs** (主表) → 各月份分区表

**分区策略**：
- commission_logs 按月份分区
- call_logs 按月份分区

**观察**：
- 分区表的设计合理，便于数据管理和查询性能优化
- 主表（如 commission_logs、call_logs）大小为0字节，符合分区表特性

## 6. Drizzle Schema 分析

### Schema文件统计
- **Schema文件数量**: 18 个
- **表定义数量**: 94 个 `export const` 定义
- **索引定义**: 需要进一步分析
- **关系定义**: 需要进一步分析

### 主要Schema分类
1. **核心表**: users, vendors, billing, agents
2. **兑换码系统**: redemption, campaigns, code-templates
3. **财务**: finance
4. **系统**: system
5. **角色权限**: roles
6. **安全**: security
7. **提示词审计**: prompt-audit
8. **通知**: notification
9. **额度**: quotas
10. **管理后台**: admin
11. **API密钥**: api-keys

## 7. 查询模式分析建议

### 潜在全表扫描风险
1. **缺少索引的查询字段**：
   - 检查 `db.select()` 无 `.limit()` 的查询
   - 检查 `db.select()` 无 `.where()` 的查询
   - 检查 `db.select()` 含 `.orderBy()` 但无对应索引的查询

2. **建议优化**：
   - 为频繁查询的字段添加索引
   - 为排序字段添加复合索引
   - 为关联查询字段添加外键和索引

## 8. 性能优化建议

### 高优先级
1. **清理未使用索引**
   - 删除 call_logs 分区表的未使用索引
   - 删除 commission_logs 分区表的未使用索引

2. **添加缺失外键**
   - 为所有 `_id` 字段添加外键约束
   - 确保数据引用完整性

3. **监控大表查询**
   - 监控 call_logs_202607 的查询性能
   - 考虑数据归档策略

### 中优先级
1. **优化索引设计**
   - 分析查询模式，优化复合索引
   - 为高频查询字段添加索引

2. **分区表维护**
   - 确保分区策略合理
   - 定期清理历史分区数据

### 低优先级
1. **Schema优化**
   - 检查数据类型是否合理
   - 优化表结构设计

## 9. 下一步行动

1. **立即执行**：
   - 删除确认未使用的索引
   - 为关键关联字段添加外键

2. **短期计划**：
   - 分析慢查询日志
   - 优化高频查询的索引

3. **长期计划**：
   - 建立数据库监控体系
   - 制定数据归档策略
   - 定期进行数据库性能分析

## 10. 分析工具使用情况

- **pg_stat_statements**: 已启用，可用于慢查询分析
- **pg_stat_user_tables**: 用于表行数统计
- **pg_stat_user_indexes**: 用于索引使用率分析
- **information_schema**: 用于元数据收集

---

**报告生成者**: database-analyzer (subagent)
**完成时间**: 2026-07-23 21:48:00 GMT+8