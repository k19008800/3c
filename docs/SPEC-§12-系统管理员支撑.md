# 功能说明书：§12 系统管理员支撑

> **📖 页面功能说明帮助**
>
> **页面用途**：系统管理员支撑 功能说明书。本文档详细定义了该模块各功能点的字段规格、校验规则、边界条件、交互方式和注意事项。
>
> **适用角色**：系统管理员（super_admin/ops）
>
> **核心操作**：
- 管理系统环境和部署配置
- 执行系统维护和健康检查
- 查看系统日志和诊断信息
>
> **注意事项**：本 SPEC 文档面向开发者和测试人员，包含字段级定义和校验规则。页面标题旁的 `[?]` 帮助气泡引用本文档内容，点击可查看当前页面功能说明。
>
> **关联组件**：页面标题右侧 `[?]` 帮助按钮，悬停/点击弹出帮助弹窗。




> **对应文档**：[`PRD-系统管理员支撑.md`](PRD-系统管理员支撑.md)

---

## 12.1 操作审计控制台

### 功能描述

系统管理员查看所有管理员操作的可审计日志，支持多维度筛选、差异对比、异常模式识别和一键回滚。

### 完成能力 / 展示效果

管理员在管理后台进入"审计日志"：

- **多维度筛选**：时间范围 / 操作人 / 操作类型 / 资源类型 / 影响范围
- **审计日志列表**：时间 | 操作人 | 操作类型 | 目标类型/ID | IP | 状态
- **操作详情**：点击查看 before/after JSON diff，变更字段高亮显示
- **异常模式识别**：系统自动标记可疑操作（如凌晨批量改配置、敏感操作）→ 异常操作列表
- **操作回滚**：对配置类变更，一键回滚到变更前状态（仅超级管理员权限）
- **导出**：CSV/JSON 两种格式

### 上下游关系

```
上游：所有操作 → 写入 operation_logs 表
下游：审计控制台 → 管理员查询
      异常检测 → 标记可疑操作
      回滚 → 恢复配置变更
```

---

## 12.2 数据库管理面板

### 功能描述

可视化查看数据库运行状态，包括表大小、索引状态、慢查询、连接数等。

### 完成能力 / 展示效果

- **表空间概览**：按大小排序展示所有表（行数/磁盘占用/增长趋势）
- **索引管理**：索引列表（大小/使用率/冗余索引检测）
- **慢查询**：慢查询日志列表 + 执行计划查看
- **连接监控**：当前连接数/活跃连接/等待连接
- **表分区状态**：分区表的各分区数据量

---

## 12.3 缓存管理控制台

### 功能描述

管理员查看和管理 Redis 缓存状态，支持清空指定缓存键。

### 完成能力 / 展示效果

- **缓存概览**：Redis 内存使用率 / Key 数量 / 命中率
- **缓存列表**：按前缀分组的 Key 列表（如 billing:*, routing:*）
- **清空缓存**：按前缀清空 / 按 Key 精确清空 / 全清
- **缓存命中率趋势图**

---

## 12.4 任务调度中心

### 功能描述

查看和管理平台所有定时任务和后台作业。

### 完成能力 / 展示效果

- **任务列表**：任务名 | 调度表达式 | 上次执行时间 | 上次结果 | 状态（运行中/暂停/失败）
- **任务详情**：执行历史（每次的执行时间/耗时/结果/错误日志）
- **手动触发**：手动立即执行一次任务
- **暂停/恢复**：控制任务调度
- **任务类型**：
  - 财务类：每日对账、月度账单生成、结算
  - 运营类：用户分层计算、召回扫描、日报推送
  - 系统类：缓存预热、数据清理、健康检查
  - 安全类：安全事件检测、风险评分更新

---

## 12.5 在线日志查看器

### 功能描述

管理员在管理后台直接查看服务器应用日志，无需 SSH 登录。

### 完成能力 / 展示效果

- **日志查看**：实时 tail 模式 / 历史搜索
- **日志筛选**：按级别（ERROR/WARN/INFO/DEBUG）、时间范围、模块
- **日志分析**：错误聚合（相同错误分组计数）、趋势图
- **日志下载**：导出筛选后的日志片段

---

## 12.6 健康检查大屏

### 功能描述

平台所有服务（API/Redis/PostgreSQL/供应商 API）的健康状态一目了然。

### 完成能力 / 展示效果

- **服务列表**：服务名 | 状态（正常/异常/离线）| 响应时间 | 上次检查时间
- **健康评分**：综合计算平台整体健康分（0-100）
- **历史趋势**：各服务健康状态变化时间线
- **故障关联分析**：当多个服务同时异常时，标注可能关联

---

## 12.7 变更计划与沙箱预览

### 功能描述

计划性变更（配置修改/功能发布）先在沙箱环境预览影响范围。变更从沙箱验证通过后经审批推送到生产环境，形成完整的变更闭环。

### 完成能力 / 展示效果

- **变更计划**：创建计划 → 填写变更内容/影响范围/回滚方案/时间窗口
- **沙箱预览**：在沙箱环境执行变更 → 验证结果 → 确认后才推生产
- **变更审批**：重要变更需审批 → 审批通过执行
- **变更记录**：历史变更列表 → 可追溯每次变更

### 沙箱→生产推送审批闭环

变更从沙箱验证通过到推送到生产的完整流程：

```
┌─ 创建变更计划 ──────────────────────────────┐
│  变更标题: [调整 deepseek-chat 加价率]        │
│  变更类型: 配置变更 / 功能发布 / 版本升级       │
│  影响范围: 影响所有 deepseek 模型调用计费       │
│  回滚方案: 回滚到 v40 配置版本                  │
│  时间窗口: 2026-07-30 23:00-01:00            │
│  创建人: admin@3cloud.ai                     │
│  状态: ⏳ 草稿                                │
└──────────────────────────────────────────────┘
         ↓
┌─ 沙箱预览 ──────────────────────────────────┐
│  1. 系统自动克隆当前生产配置到沙箱环境          │
│  2. 在沙箱执行变更                            │
│  3. 自动运行验证脚本:                         │
│     ├── deepseek-chat 调用返回正确加价后价格 ✓  │
│     ├── 边缘价格计算通过 ✓                     │
│     └── 无异常日志产生 ✓                      │
│  4. 沙箱验证报告生成：3/3 测试通过             │
│  状态: ✅ 沙箱验证通过                         │
└──────────────────────────────────────────────┘
         ↓
┌─ 提交审批 ──────────────────────────────────┐
│  审批人: 需 2 人审批（super_admin 或 admin）  │
│                                              │
│  审批人 1: ________   [通过/驳回/需修改]       │
│  审批意见: ________________________________  │
│                                              │
│  审批人 2: ________   [通过/驳回/需修改]       │
│  审批意见: ________________________________  │
│                                              │
│  审批通知渠道: [站内通知] [邮件] [企微群]       │
│  超时自动批准: 24 小时无响应自动通过            │
└──────────────────────────────────────────────┘
         ↓ 审批通过
┌─ 推送生产 ──────────────────────────────────┐
│  1. 自动备份当前生产配置（快照版本）             │
│  2. 执行变更推送                              │
│  3. 自动运行生产验证脚本                       │
│     ├── 验证通过 → 变更完成                    │
│     └── 验证失败 → 自动触发回滚 → 通知管理员    │
│  4. 变更完成通知 → 所有管理员收到通知           │
│  ┌─ 如失败：自动回滚 ──────────────────┐     │
│  │  回滚到变更前生产配置快照               │     │
│  │  触发告警通知运维：'变更失败已自动回滚'  │     │
│  └──────────────────────────────────┘     │
└──────────────────────────────────────────────┘
         ↓
┌─ 变更完成 ──────────────────────────────────┐
│  变更记录编号: CHG-20260730-001               │
│  变更内容: deepseek-chat 加价率 15%→10%       │
│  执行时间: 2026-07-30 23:05                   │
│  执行耗时: 5 分钟                             │
│  执行状态: ✅ 成功                             │
│  变更后配置版本: v43                           │
│  回滚就绪: ✅ 可一键回滚至 v40                  │
└──────────────────────────────────────────────┘
```

### 变更类型与审批策略

| 变更类型 | 沙箱验证要求 | 审批人数 | 通知范围 | 回滚策略 |
|---------|------------|---------|---------|--------|
| 配置参数修改 | 自动执行 | 1 人 | 变更发起人 | 一键回滚上一个版本 |
| 模型价格变更 | 自动验证 + 手动确认 | 2 人 | 运营 + 财务 | 自动回滚 + 价格检查 |
| 路由策略调整 | 运行测试用例 | 2 人 | 运维 + 运营 | 自动回滚 + 熔断恢复 |
| 功能发布（前端） | 自动 E2E 测试 | 1 人 | 相关方 | 前端版本回滚 |
| 功能发布（后端） | 自动 E2E 测试 + 性能测试 | 2 人 | 所有管理员 | 数据库迁移回滚 |
| 数据库结构变更 | 需 DBA 审核 | super_admin 仅 | 所有管理员 | 迁移回滚脚本执行 |

### 变更记录查询

```
变更列表（近 30 天）：
  2026-07-30 CHG-001  调整 deepseek-chat 加价率   ✅ 成功  15分钟
  2026-07-28 CHG-002  新增供应商 OspreyAI        ✅ 成功  30分钟
  2026-07-25 CHG-003  全局限流参数调整             ⚠️ 自动回滚  2分钟
                                                        原因：验证失败

点击任一行 → 查看变更详情（含审批记录、沙箱报告、回滚状态）
```

---

## 12.8 系统升级与版本管理

### 功能描述

管理平台版本升级流程。

### 完成能力 / 展示效果

- **版本列表**：版本号 | 发布时间 | 变更日志 | 当前生产版本
- **版本部署**：一键部署到指定环境（需审批）
- **回滚**：一键回滚到上一个版本
- **版本差异**：展示两个版本之间的代码/配置差异

---

## 12.9 系统管理员模块总览

| 模块 | 优先级 | 预估工作量 | 核心价值 |
|------|--------|-----------|---------|
| 操作审计控制台 | P0 | 后端 3d + 前端 4d | 操作追责+异常模式识别，安全合规 |
| 健康检查大屏 | P0 | 后端 3d + 前端 4d | 一眼掌握系统状态，故障快速定位 |
| 任务调度中心 | P1 | 后端 4d + 前端 4d | 定时任务统一管理，失败及时发现 |
| 在线日志查看器 | P1 | 后端 3d + 前端 3d | 无需 SSH 即可查看日志，安全便捷 |
| 缓存管理控制台 | P1 | 后端 2d + 前端 3d | 缓存可视化，按需清理 |
| 数据库管理面板 | P2 | 后端 4d + 前端 4d | 数据库状态可视化，辅助性能调优 |
| 变更计划与沙箱预览 | P2 | 后端 4d + 前端 4d | 降低变更风险，支持计划编排 |
| 系统升级与版本管理 | P2 | 后端 3d + 前端 3d | 版本可追溯，一键回滚，灰度发布 |

**合计**：后端 26 人天 + 前端 29 人天 = 约 7 周

> 权限说明：系统管理员模块操作影响系统稳定性，所有 API 均需超级管理员权限。部分高危操作（如回滚/部署/执行 SQL）需额外二次确认弹窗。

### 系统工具权限矩阵（与 §30 权限系统集成）

每个系统工具子模块需要绑定到 §30 的权限资源上，以便在自定义角色中精细化控制访问。

| 模块 | 资源标识 | 可操作角色 | 高危操作 | 二次确认要求 |
|------|---------|-----------|---------|------------|
| 操作审计控制台 | `admin_tools:audit` | super_admin / admin / auditor | 操作回滚 | 需二次确认弹窗 + 输入登录密码 |
| 数据库管理面板 | `admin_tools:database` | super_admin 仅可查看，不可执行 SQL | 慢查询查看、表空间查看 | 查看慢查询需二次确认 |
| 缓存管理控制台 | `admin_tools:cache` | super_admin / admin | 清空指定缓存、全清缓存 | 清空缓存需二次确认；全清需双人审批 |
| 任务调度中心 | `admin_tools:scheduler` | super_admin / admin | 手动触发任务、暂停/恢复任务 | 暂停关键任务需二次确认 |
| 在线日志查看器 | `admin_tools:logs` | super_admin / admin / ops | 无（只读） | 无 |
| 健康检查大屏 | `admin_tools:health` | super_admin / admin / ops | 无（只读） | 无 |
| 变更计划与沙箱预览 | `admin_tools:changes` | super_admin / admin | 沙箱→生产推送 | 需审批流程（见 §12.7 变更管理）|  
| 系统升级与版本管理 | `admin_tools:upgrade` | super_admin 仅 | 版本部署、一键回滚 | 回滚需二次确认 + 输入登录密码；版本部署需双人审批 |

### 权限边界条件

| 场景 | 行为 |
|------|------|
| admin 角色尝试清空全部缓存 | 弹窗：

---

### [?] 页面帮助

**页面名称**：系统管理员支撑

**适用角色**：系统管理员

**功能定位**：为系统管理员提供平台日常运维支撑工具：系统监控、日志管理、配置管理、公告管理等，保障平台稳定运行。

**子模块说明**：
- §12.1 系统监控：服务器/服务/依赖组件运行状态监控
- §12.2 日志管理：系统日志查看、检索、导出
- §12.3 配置管理：系统级配置项查看与修改
- §12.4 公告管理：平台公告发布与管理

**注意事项**：
- 系统配置修改需谨慎操作，部分配置修改后需重启服务生效
- 日志保留策略按配置执行，敏感日志访问需审计
- 监控告警阈值可配置，告警通知通道需提前配置

**常见问题**：
Q: 服务告警了怎么处理？
A: 查看监控面板和日志定位问题，按运维手册处理，必要时重启服务。

Q: 修改配置后不生效？
A: 部分配置项修改后需要重启服务或刷新缓存，具体见配置说明。

### [?] 按钮级帮助对照表

**§12.1 系统监控**

| 按钮/操作 | 帮助说明 |
|----------|---------|
| 查看监控面板 | 查看服务器/服务/依赖的实时状态 |
| 设置告警阈值 | 配置各项指标的告警阈值 |
| 查看告警历史 | 查看历史告警记录 |

**§12.2 日志管理**

| 按钮/操作 | 帮助说明 |
|----------|---------|
| 检索日志 | 按关键字/时间/级别检索日志 |
| 导出日志 | 导出检索结果 |
| 日志级别设置 | 调整日志输出级别 |

**§12.3 配置管理**

| 按钮/操作 | 帮助说明 |
|----------|---------|
| 查看配置 | 查看系统配置项及当前值 |
| 修改配置 | 修改系统配置项（需确认） |
| 重启服务 | 配置生效需重启时执行 |

**§12.4 公告管理**

| 按钮/操作 | 帮助说明 |
|----------|---------|
| 发布公告 | 创建并发布平台公告 |
| 编辑公告 | 修改已发布公告 |
| 下线公告 | 下线不再需要的公告 |


---

## 边界条件

### 系统级边界条件

| # | 场景 | 触发条件 | 预期行为 |
|---|------|---------|---------|
| SYS-001 | 审计日志容量告警 | 审计日志占用存储超过阈值的 80% | 系统触发告警通知 super_admin，自动归档 30 天前的日志
| SYS-002 | DB 管理面板连接超时 | DB 管理面板在空闲 15 分钟后尝试执行 SQL | 会话自动断开，需重新认证后可继续操作
| SYS-003 | 任务调度并发限制 | 同一时间内触发的后台任务超过最大并发数（10） | 新任务进入等待队列，按优先级依次执行
| SYS-004 | 系统升级期间拒绝关键操作 | 版本升级过程中用户尝试执行数据变更操作 | 弹窗提示"系统升级中，请稍后再试"并阻止操作
| SYS-005 | 健康检查大屏数据延迟 | 健康检查大屏数据超过 30 秒未刷新 | 前端显示旧状态并标记"数据延迟"警告
