# 测试报告 — 整改阶段一 R1–R4（+D1）验收测试（Gate 4）

| 字段 | 值 |
|------|-----|
| 报告编号 | TEST-RECT-P0-R1R4-001（v1.1，含修复回归结论） |
| 测试执行 | test-agent（专职测试工程师，Gate 4） |
| 执行日期 | 2026-08-18（首轮）；2026-08-18（修复回归） |
| 被测版本 | 工作区 HEAD `bfb6f84` + R1–R4/D1 未提交改动（backend-agent 交付 + 修复轮） |
| 上游契约 | `docs/PRD-整改R1-R4-人工上账与资金入账.md`（§6 验收清单、§12.2 裁决调整）、`docs/ARCH-整改R1-R4-技术方案.md`（§8 测试清单、§12 双签裁决、§12.4 D1/D2）、`docs/整改方案-管理员充值业务演练.md`（§六 验收 Checklist） |
| 任务分配单 | `docs/整改R1-R4-任务分配单.md`（Gate 4 状态更新） |
| 测试方法 | 真实 PG + Redis 集成测试（vitest run，`buildTestApp()` + `app.inject`），代码走查，临时补验脚本（跑完已删） |

---

## 1. 测试范围与环境

### 1.1 测试范围

| 编号 | 范围 | 说明 |
|------|------|------|
| R1 | 人工上账创建入口 | `POST /admin/manual-topup` 创建、校验、幂等（L1 Redis 锁 + L2 唯一列）、审核（approve/reject）、审计、MT 订单号 |
| R2 | 余额账户兜底 | `creditBalance()` 统一收口：人工上账审核 / 调账生效（免审批+一级+二级+红冲加钱）/ 充值订单 audit 三条加钱路径；无余额行自动建户；并发不丢更新 |
| R3 | 权限点鉴权对齐 | `requirePerm('finance.topup'/'finance.adjust')` 替换资金路由 adminAuth；`finance.adjust` 权限点新增；前端 `usePerm` 显隐（D2 财务导航）；D1 轻量用户搜索端点 |
| R4 | 入账通知闭环 | `notifyUser`：站内信写 `notifications`（type=`recharge_success`，必发）+ 邮件按策略/模板；无模板 `no_template` 降级；通知失败不影响主交易 |
| D1 | 用户搜索端点 | `GET /admin/manual-topup/users?search=`（邮箱模糊/ID 精确/手机号），finance 可用、sales 403 |
| 回归 | 全量 | 仓库根 `pnpm test`（api vitest，62 文件） |

### 1.2 测试环境

- 真实 PostgreSQL + Redis 测试实例（复用 `admin-risk-finance.test.ts` 风格：`buildTestApp()` 最小 Fastify 实例 + `app.inject` + 独立时间戳隔离 + `afterAll` 清理）；
- JWT 三角色 token 签发（admin / finance / sales / customer）；finance 角色在 DB 中为 customer 行、JWT `role='finance'`（与既有测试基建一致）；
- 未改任何业务代码；未绕权限直改数据库"通过"验收——测试基建建数据（插入测试用户/订单）均已在报告说明；
- 临时补验脚本（`__acceptance-verify*.test.ts`）运行后已删除，不污染交付。

### 1.3 测试执行清单

| 类型 | 文件/动作 | 结果 |
|------|----------|------|
| 全量回归（默认并发） | 仓库根 `pnpm test` | 62 文件：7 失败 / 55 通过；953 用例：858 通过 / 95 跳过 |
| 失败文件串行复跑 | 7 个失败文件 `--pool=forks --poolOptions.forks.singleFork` | **7 文件 112 用例全部通过** |
| 全量回归（受限并发 `--maxWorkers=4`） | vitest run | **62 文件 953 用例全部通过** |
| web-console typecheck | `tsc --noEmit` | **0 错误** |
| 验收补验脚本 V1–V7 | 临时测试（已删） | 见 §3/§4 |
| **修复回归：复验脚本 R1–R5** | 临时测试（已删） | **5/5 通过（D-01/D-02/D-04/D-06/P1-3）** |
| **修复回归：全量（`--maxWorkers=4`）** | vitest run | **62 文件 958 用例全部通过** |
| **修复回归：api typecheck** | `tsc --noEmit` | **0 错误** |

---

## 2. 全量回归结果

### 2.1 默认并发首次运行（仓库根 `pnpm test`）

```
Test Files  7 failed | 55 passed (62)
Tests       858 passed | 95 skipped (953)
```

失败文件（全部为 `buildApp` beforeAll hook 超时 `Hook timed out in 10000ms`，非断言失败）：

| # | 失败文件 | 失败点 | 归属 |
|---|---------|--------|------|
| 1 | test/admin-marketplace.test.ts | beforeAll buildApp 超时 | 存量（P2-2） |
| 2 | test/agent-commission.test.ts | beforeAll buildApp 超时 | 存量 |
| 3 | test/agent-materials.test.ts | beforeAll buildApp 超时 | 存量 |
| 4 | test/billing.test.ts | beforeAll buildApp 超时 | 存量 |
| 5 | test/i18n-blog.test.ts | beforeAll buildApp 超时 | 存量 |
| 6 | test/vendor-settlement.test.ts | beforeAll buildApp 超时 | 存量 |
| 7 | src/routes/vendor-alias.test.ts | beforeAll buildApp 超时 | 存量 |

**失败判定**：7 个失败文件均为存量测试，且失败点全部在 `buildApp()` 全量装配（hook 超时 10s），无一为断言/逻辑失败；串行复跑 7 文件 **112/112 全过**；受限并发（4 worker）全量 **62 文件 953 用例全过**。**结论：默认并发下 7 文件失败为并行资源竞争导致的 hook 超时（环境问题），非本次改动引入，无回归。**

### 2.2 受限并发复跑（`--maxWorkers=4`，确认稳定）

```
Test Files  62 passed (62)
Tests       953 passed (953)
Duration    94.71s
```

### 2.3 本次改动新增测试（Gate 2 交付的 5 个测试文件）

| 文件 | 覆盖 | 结果 |
|------|------|------|
| `api/src/routes/admin-manual-topup.test.ts` | ARCH §8 用例 1–7 + D1 | ✅ 全部通过（含幂等 L1/L2、兜底、通知、越权） |
| `api/src/routes/admin-adjust.test.ts` | 用例 11/14（调账收口 + finance 越权 403） | ✅ |
| `api/src/routes/admin-recharge-audit.test.ts` | 用例 12（充值 audit 收口 + 通知 + finance 放行） | ✅ |
| `api/src/services/billing/balance.test.ts` | 用例 8–10（creditBalance 兜底/正常/并发 10 次不丢更新） | ✅ |
| `api/src/middleware/require-perm.test.ts` | 用例 13（权限矩阵 + 中间件行为） | ✅ |

> 对照存量基线：本次为 951 用例全过（backend 自测报告），本次实测 953 用例（受限并发全过）——多出的用例为新增 5 测试文件覆盖，**无存量用例失败**。

---

## 3. 业务场景验证结果表（PRD §6 验收项 × 结果 × 证据）

图例：✅ 通过（自动化/代码验证）｜❌ 不通过（缺陷）｜⚠️ 人工验收（本环境无法验证）

### 3.1 R1 人工上账创建入口

| # | PRD 验收项 | 结果 | 证据 |
|---|-----------|:---:|------|
| R1-1 | "发起上账"入口（finance.topup 可见），点击弹表单 | ✅ | 前端 `AdminManualRechargePage.tsx`：`usePerm("finance.topup")` 控制按钮渲染 + 表单弹窗 + 二次确认弹窗；按钮旁 `[?]` HelpIcon（P1 合规） |
| R1-2 | 创建成功 → pending 出现在列表（用户/金额/类型/原因） | ✅ | 用例 1：201，`method=manual`、`status=pending`、`order_no` 以 MT 开头、`metadata.transfer_no` 正确；GET 列表 pending 页签可见 |
| R1-3 | 字段校验：用户不存在/金额≤0/>50,000/原因为空/对公缺凭证单号/单号重复/凭证格式不符均拦截 | ⚠️/❌ | 自动：金额 0/负/超上限 400、缺 note 400、用户不存在 404、frozen 400、`evidence_url` 传入 400、金额超 2 位小数 400（补验 V3）✅；**单号重复未拦截（缺陷 D-01）❌**；凭证上传属 R9 后置，格式/大小校验本环境无法验证（无上传能力，人工验收） |
| R1-4 | 对公凭证/单号必填；grant 选填 | ⚠️ | 按 ARCH §12.2 A6 裁决：本期无凭证上传能力，"对公必填"校验后置 R9；`transfer_no` 可选写入 metadata。前端表单保留类型选择（仅提示）。**单号唯一性裁决未落实（缺陷 D-01）** |
| R1-5 | 同一转账单号重复创建被拒 | ❌ | **缺陷 D-01**：补验 V1 实测同 `transfer_no` 不同幂等键两次创建均 201，DB 2 行（与 ARCH §12.2"transfer_no 填写则全平台唯一"及 PRD C9/B4 不符） |
| R1-6 | 审核通过 → paid + 余额增加 + 流水；驳回 → failed + 驳回原因 | ✅/❌ | 通过：用例 4/5（approve → paid、余额+amount、流水 type=recharge、balance_after 快照正确）✅；驳回：状态置 failed ✅，**但驳回原因非必填且覆盖原 note（缺陷 D-02）❌** |
| R1-7 | 二次确认与 toast；状态机防重复审核 | ✅ | 前端通过/驳回弹窗（驳回必填原因前端 disabled 拦截）+ toast；后端 `where status='pending'` 原子守卫，重复 approve → 409（用例 5） |
| R1-8 | 审核人（reviewer_id）在审核时落库 | ✅ | 用例 5：approve 后 `metadata.reviewer_id` = 审核人 id（裁决 A8：落 metadata） |
| R1-9 | 创建/审核均写审计日志 | ✅ | 创建 `manual_topup.create`（用例 1：details 含 userId/amount/order_no/created_by）；审核 `manual_topup.approve`（含 notification 状态）/ `manual_topup.reject` |
| R1-10 | 幂等：同 Idempotency-Key 重复提交仅 1 行 | ✅ | 用例 3：首请求 201；重放 L1 缓存命中 → 200 + `X-Idempotent-Replay:true` 或 409；杀缓存后 L2 唯一约束 409；DB 均仅 1 行 |

### 3.2 R2 余额账户兜底

| # | PRD 验收项 | 结果 | 证据 |
|---|-----------|:---:|------|
| R2-1 | 无余额行用户人工上账审核通过 → 入账成功不再 404 | ✅ | 用例 4：无 `customer_balances` 行用户 approve → 200，自动建行且余额=amount，流水 1 条 |
| R2-2 | 调账生效 / 充值审核同样兜底 | ✅ | 用例 11（调账免审批调增无余额行建户）；用例 12（充值 audit 无余额行建户） |
| R2-3 | 并发建户不重复、不覆盖；并发入账不丢更新 | ✅ | `balance.test.ts` 用例 10：同用户并发 10 次 creditBalance → 余额=10×amount、流水 10 条（复核断言有效：`Promise.all` 各自事务 + 行级锁串行化） |
| R2-4 | 无余额行用户登录用户端余额展示 ¥0.00 不报错 | ✅ | 代码走查：`getBalance()` 对无行返回 `{0,0,0,'CNY'}`（现状既有行为，未改动，回归通过） |
| R2-5 | 自动建户事件写审计（来源标识 auto_create） | ⚠️ | 实现：`creditBalance` 内 `INSERT ... ON CONFLICT DO NOTHING` 建户，建户随入账事务提交；**审计/日志中未显式带 `auto_create` 来源标识**（PRD §3.2.1-6 要求"建户事件须写入审计/日志（来源标识 auto_create）"）——建户零金额不改合计口径，无资金影响，但审计标识缺失，记 **P3 观察**（见缺陷台账 D-05 附录）；对账金额口径无变化 ✅ |
| R2-6 | 事务原子：建户+增额+流水整体回滚 | ✅ | `balance.test.ts` 用例 8：事务内入账后抛错 → 建行/增额/流水全部回滚 |

### 3.3 R3 权限点鉴权对齐

| # | PRD 验收项 | 结果 | 证据 |
|---|-----------|:---:|------|
| R3-1 | finance 可发起、可审核人工上账；前端可见入口 | ✅ | 用例 7（finance 创建 201 / 审核 200）；前端 `usePerm("finance.topup")` 渲染入口 + D2 财务导航（`ConsoleLayout.tsx` FINANCE_NAV） |
| R3-2 | finance 不可发起调账：前端无入口，直接调用 403 | ✅ | 用例 14（finance POST /admin/adjust → 403）；前端 `AdminAdjustPage.tsx` finance 角色整页 EmptyState（无假入口） |
| R3-3 | 无 finance.topup 角色前端无入口、调用 403 | ✅ | 用例 7（sales 创建 403 / customer 列表 403）；前端无权限 EmptyState |
| R3-4 | 越权统一 403 + 规范文案 | ✅ | `requirePerm` 抛 `ForbiddenError`（403 `FORBIDDEN`）；前端 `extractError` 统一提示 |
| R3-5 | 权限树新增 finance.adjust（仅 admin/super_admin）；权限详情正确反映 | ✅ | `lib/permissions.ts` PERM_GROUPS 财务组含 `finance.adjust`；ROLE_PERMS.admin 含、finance 不含；`admin-permissions.ts` 改从 lib 导入（单源）；用例 13 矩阵断言 |
| R3-6 | admin/super_admin 全部资金操作可用（回归） | ✅ | admin 创建/审核/调账/充值审核全部通过；super_admin `'*'` 通配放行（用例 13） |
| R3-7 | 充值订单审核（audit/reject）finance 可用（裁决 A4/Q2） | ✅ | `admin-recharge-audit.test.ts` 用例 12：finance audit 200 / reject 200 |
| R3-8 | D1：finance 搜索（邮箱/ID/手机号）；sales 403 | ✅ | 用例 D1：邮箱模糊命中 / ID 精确命中 / 手机号命中（补验 V4）/ 空列表；sales 403 |
| R3-9 | 前端按钮显隐 + `[?]` 帮助（P1 合规） | ✅ | 发起上账/审核通过/驳回/用户搜索/金额/凭证/单号等按钮均带 `HelpIcon` tooltip；页面级 `PageHelp`；文案对齐 PRD §7/§8（代码层验证，视觉人工验收） |

### 3.4 R4 入账通知闭环

| # | PRD 验收项 | 结果 | 证据 |
|---|-----------|:---:|------|
| R4-1 | 人工上账审核通过 → 站内信（必发）+ 邮件（按偏好，默认发） | ✅ | 用例 6：`notifications` 落库 `type='recharge_success'`（标题"充值到账通知"、正文含金额/余额）；无模板 → `email='no_template'`；有模板 + SMTP 未配置 → `email='skipped'` 不报错 |
| R4-2 | 调账生效（调增）→ 同样通知 | ✅ | 补验 N2-a（免审批调增生效 → recharge_success 落库）/ N2-b（一级审批调增生效 → 落库）；代码走查二级 review 同路径（`applyApproval` 调增方向） |
| R4-3 | 充值订单审核通过 → 同样通知 | ✅ | 用例 12：audit 后 notifications 有 `recharge_success` |
| R4-4 | 通知文案含金额/余额/时间/类型（模板变量齐全） | ⚠️ | 站内信 content 含金额 + 余额（用例 6 断言 `66.00`）✅；**时间、类型变量在 content 模板中未体现**（content 为固定句式 `已入账 ¥X，当前余额 ¥Y`，无时间/类型字段）；`templateVars` 提供 amount/balance_after/order_no（无 time/type_label）——与 PRD §3.4.3 模板变量建议不完全一致，记 **P3 观察**（模板由运营侧维护，可后续补变量） |
| R4-5 | 用户关闭邮件偏好 → 仅站内信；站内信不可关闭 | ✅ | 实现：站内信写 `notifications` 无偏好判断（强制必发）；邮件按 `notification_policies_list` 策略（channel=email && event_type=event 且 enabled=false 时跳过，默认发送）——裁决 Q3/Q4 口径（`user_notifications` 更正为 `notifications`） |
| R4-6 | 通知失败不影响入账；通知记录（失败/重试）可查 | ✅ | 代码走查：`notifyUser` 站内信/邮件逐通道 try/catch，失败仅记日志/返回状态不抛错（调用点在资金事务 COMMIT 之后）；`email_logs` 由 `sendMail` 写；SMTP 未配置返回 `skipped` 不抛错 |
| R4-7 | 事件名统一 `recharge_success`，与 SPEC-§22 一致 | ✅ | 三处调用点统一 `event='recharge_success'`（裁决 Q4 修正 ARCH §6 双命名） |

### 3.5 整改方案 §六 验收 Checklist（演练文档）

| # | Checklist 项 | 结果 | 证据 |
|---|--------------|:---:|------|
| C-1 | 管理员可为无余额行用户直接充值 ¥1,000，不再 404 | ✅ | 用例 4/11/12（三条路径兜底） |
| C-2 | 人工上账：可创建 → 待审 → 审核通过 → 余额增加 → 通知 → 台账可见 | ✅ | 用例 1/4/6 + 列表可见 + 通知落库 |
| C-3 | finance 可审核人工上账；发起调账 403 | ✅ | 用例 7/14 |
| C-4 | 调增 ≥¥10,000 触发一级审批；审批人 ≠ 申请人 | ✅（现状回归） | `calcApproval` 阈值现状保留；`admin-adjust.ts` 职责分离校验（审批人≠申请人） |
| C-5 | 同一操作人 24h 内 9 次 ¥9,999 触发强制审批 | ⚠️ | 属 R6（二期），本期不做；回归验证现状无拆分规避 |
| C-6 | 资金写操作未通过 2FA 被拒 | ⚠️ | 属 R7（二期），本期不做（PRD 明确预留）；前端二次确认已实现 |
| C-7 | platform_ledger 出现 user_recharge/internal_adjust | ⚠️ | 属 R8（二期），本期不建表（ARCH §10 预留钩子在 creditBalance） |
| C-8 | 凭证/转账单号/审核人全部落库可查 | ⚠️/❌ | reviewer_id 落 metadata ✅；transfer_no 落 metadata ✅ 但**无唯一性约束（缺陷 D-01）**；凭证上传属 R9（人工验收） |
| C-9 | 重复提交（双击/重试）只生效一次 | ✅ | 幂等键 L1+L2（用例 3） |
| C-10 | 错误入账红冲有独立审批，审计含 diff | ⚠️ | 红冲保留现状（R12 二期）；红冲加钱方向已收口 creditBalance（用例 11 补验） |
| C-11 | 新页面/按钮全部带 `[?]` 帮助（P1 合规） | ✅（代码层） | 前端 HelpIcon 全覆盖；视觉/交互人工验收 |

### 3.6 需人工验收项（本环境无法验证，交付/调度安排）

| # | 项 | 原因 |
|---|-----|------|
| M-1 | 真机交互：finance 登录 → 侧边栏财务导航 → 人工上账页 → 搜索选人 → 发起上账 → 二次确认 → 提交 → 列表置顶 pending | 需浏览器真机联调（前端已 typecheck/build 0 错误） |
| M-2 | SMTP 真实邮件外发（模板上线后） | 测试环境无 SMTP；站内信与邮件降级路径已自动验证 |
| M-3 | 前端 E2E（Playwright） | 本项目尚无 E2E 基建 |
| M-4 | 凭证上传（JPG/PNG/PDF ≤5MB）格式/大小校验 | R9 后置，本期无上传能力 |
| M-5 | 用户端通知中心站内信展示效果 | 需用户端真机 |

---

## 4. 缺陷台账

### P0 阻断

无。

### P1 严重（1 项）

| ID | 标题 | 复现步骤（或代码位置） | 预期 | 实际 | 严重级依据 | **状态** |
|----|------|----------------------|------|------|-----------|---------|
| D-01 | **`transfer_no` 全平台唯一性未实现，同一转账单号可重复创建** | `api/src/routes/admin-finance-missing.ts` POST `/manual-topup`（307-433 行）：校验仅含长度 ≤100（332 行），无 `transfer_no` 查重；metadata 为 jsonb 无唯一约束。补验 V1：对同一 active 用户、同 `transfer_no`、不同 Idempotency-Key 连续创建两笔 | 第二笔被拒（400/409，"该转账单号已存在上账记录"） | 两笔均 201，`recharge_orders` 中同 `transfer_no` 订单 2 行 | 与 ARCH §12.2 裁决"transfer_no 填写则全平台唯一"及 PRD C9/B4、整改方案 R1 验收"重复创建同单号被拒"**直接不符**；防重复入账第一道闸失效，同一银行流水可被重复上账（资损风险），虽审核为人工第二道闸，仍属资金安全缺陷 | **✅ 已关闭（修复回归通过，见 §6）** |

### P2 一般（1 项）

| ID | 标题 | 复现步骤（或代码位置） | 预期 | 实际 | 严重级依据 | **状态** |
|----|------|----------------------|------|------|-----------|---------|
| D-02 | **驳回原因非必填（后端未拦截），且驳回覆盖/清空原入账原因 note** | `admin-finance-missing.ts` review reject 分支（446-455 行）：`set({ status:'failed', note: body.note?.trim() || null })`，无 note 必填校验。补验 V2：reject 不传 note → 200，订单 note 变 null（原入账原因丢失） | reject 不填原因 → 400 拦截（PRD B17）；驳回原因独立记录、原入账原因保留（PRD §3.1.2 review_note/note 两字段） | reject 无原因返回 200；原 note（入账原因）被驳回原因覆盖或清空为 null | 与 PRD B17"驳回时未填原因 → 拦截，驳回原因必填"、字段表 `review_note` 必填不符；原入账原因丢失影响追溯（列表驳回原因列展示的是 note 覆盖值）。前端有 disabled 拦截仅为 UI 层，后端不设防可绕过 | **✅ 已关闭（修复回归通过，见 §6）** |

### P3 建议 / 观察（5 项）

| ID | 标题 | 说明 | 依据 | **状态** |
|----|------|------|------|---------|
| D-03 | 列表契约 `transfer_no`/`reviewer_id` 恒为 null | 创建含 transfer_no 的订单后，GET 列表仍返回 `transfer_no:null, reviewer_id:null`（补验 V5）。ARCH §9.1 明确"GET 列表契约不变（占位 null 保持）"故实现符合裁决，但 PRD §3.1.2 字面"本期随创建/审核能力落地"未完全达成——管理员在列表无法核验单号/审核人，需进库查。建议 R9 随凭证方案补齐列表字段 | 口径差异，双签裁决优先，非阻断 | 观察项，留 R9 |
| D-04 | 充值订单 reject 无审计记录 | 充值订单 `reject`（recharge.ts 451-464）仅置 failed，未写 `audit_logs`（补验 V6：审计 0 条）；人工上账 reject 有审计，不对称 | ARCH §6.3 仅要求 audit 通过写审计；建议补 reject 审计留痕 | **✅ 已关闭（修复回归通过，见 §6）** |
| D-05 | 调账红冲（加钱方向）生效无通知 | 红冲 reverse 加钱方向（admin-adjust.ts 527-530）仅同步 Redis 热账本，未调 `notifyUser`（补验 V7：通知 0 条）。ARCH §6.3 调用点表仅列免审批/一级/二级 3 个入口，未含红冲 → 实现符合 ARCH，但业务上原调减被冲回属"余额增加"，用户无通知感知 | 与 ARCH 一致，二期 `adjustment_success` 事件细分时评估 | 观察项，留二期 |
| D-06 | 自动建户审计缺少 `auto_create` 来源标识 | `creditBalance` 建户（INSERT ON CONFLICT DO NOTHING）无独立审计/日志标识（PRD §3.2.1-6 要求"建户事件须写入审计/日志（来源标识 auto_create）"）；入账审计含 balance_before 空但无建户标识。零金额操作，无资金影响 | 建议在建户分支补日志/审计标记，供对账定位 | **✅ 已关闭（修复回归通过，见 §6）** |
| D-07 | 默认并发全量回归 7 文件 hook 超时（环境观察，非代码缺陷） | 默认并行执行时 7 个存量集成测试 `buildApp` beforeAll 超时（10s）；串行复跑 112/112 通过、受限并发 953/953 通过。判定为并行资源竞争 | 建议 CI 限制 vitest 并发（`--maxWorkers`）或调大 `hookTimeout`，避免偶发红 | 环境观察（本次复跑 958/958 全过，未再复现） |

---

## 5. 验收结论（Gate 4）

### 首轮结论：**不通过（需修复后回归）** → 修复回归后：**✅ 通过（Gate 4 放行）**

| 维度 | 首轮结果 | 修复回归后 |
|------|----------|-----------|
| 全量回归 | ⚠️ 受限并发 62 文件 / 953 用例全过（默认并发 7 文件超时为环境问题） | ✅ 62 文件 / **958 用例全过**（`--maxWorkers=4` 单实例） |
| typecheck | ✅ 后端 0 错误、前端 web-console 0 错误 | ✅ api `tsc --noEmit` 0 错误（本次复跑确认） |
| R1 创建/审核/审计/幂等 | ⚠️ 主体 ✅，**D-01 阻断"同一转账单号重复创建被拒"** | ✅ D-01 已关闭（同单号跨幂等键 409 `TRANSFER_NO_DUPLICATE`、不同单号可创建、0028 部分唯一索引兜底实测生效） |
| R1 驳回 | ❌ D-02（驳回原因非必填 + 覆盖原 note） | ✅ D-02 已关闭（无原因 400；有原因 200 + 原 note 保留 + `metadata.review_note` 独立 + 列表 review_note 正确展示） |
| R2 余额兜底 | ✅ 三条路径 + 并发全部通过 | ✅ 958 用例含 D-06 复验（建户 audit `balance.auto_create` source=auto_create、有行不产生） |
| R3 权限 | ✅ 全部通过（含 D1 搜索、D2 导航代码验证） | ✅ 含 P1-3 复验（finance GET /admin/recharge-orders 200；sales 403） |
| R4 通知 | ✅ 三条触发点 + 降级全部通过 | ✅ 958 用例全过 |
| P1 合规 `[?]` | ✅ 代码层全覆盖（视觉人工验收） | ✅ 保持 |

**首轮不通过理由**：
1. **D-01（P1）**：`transfer_no` 全平台唯一性是整改方案 R1 验收原文、PRD C9/B4、ARCH §12.2 双签裁决三重要求的硬性验收项，实测可同单号重复创建两笔订单。按任务书"如发现实现与 ARCH §12 裁决不符，一律记为缺陷"，且属资金安全（防重复入账第一道闸失效），**Gate 4 判定不通过**。
2. **D-02（P2）**：驳回原因必填为 PRD 明确验收项，后端未拦截且覆盖原入账原因，需修复。

**修复确认（backend-agent 修复轮，见 §6 修复回归）**：D-01 / D-02 / D-04 / D-06 + review P1-3 全部复验通过，全量 958/958、typecheck 0 错误 → **Gate 4 转 ✅ 通过**。

---

## 6. 修复回归结论（backend-agent 修复轮复验）

> 执行：test-agent 2026-08-18；方法：临时复验脚本（R1–R5，跑完已删）+ 受限并发全量 + typecheck；未改业务代码。

### 6.1 修复内容核对（代码走查）

| 缺陷 | 修复实现（证据） |
|------|-----------------|
| D-01 | ①创建端点应用层查重：`metadata->>'transfer_no' = ?` 命中即 409 `TRANSFER_NO_DUPLICATE`（`admin-finance-missing.ts:344-352`）；②新增 migration **0028** `uq_recharge_orders_transfer_no` 表达式部分唯一索引（`metadata->>'transfer_no' IS NOT NULL`），并发窗口兜底（catch 23505 → 409，`admin-finance-missing.ts:446-452`）；③schema 同步声明（`recharge-orders.ts` transferNoUnique） |
| D-02 | ①reject 分支强制 `note` 非空 → 400 `VALIDATION_ERROR`（`admin-finance-missing.ts:468-470`）；②原 note（入账原因）保留不动，驳回原因独立写 `metadata.review_note`（477 行）；③GET 列表 failed 行 review_note 从 metadata 读取展示（247-248 行） |
| D-04 | 充值订单 reject 补审计：`action='recharge_order.reject'`，details 含 userId/order_no（`recharge.ts:462-471`） |
| D-06 | `creditBalance` 建户分支（`INSERT ON CONFLICT DO NOTHING` 实际插入时）同事务写 `audit_logs`：`action='balance.auto_create'`、`details.source='auto_create'` + reference_type/reference_id（`balance.ts:86-101`）；已有行（onConflict no-op）不产生 |
| P1-3 | `GET /admin/recharge-orders` 鉴权换 `requirePerm('finance.topup')`（`recharge.ts:305`），finance 可查看列表 |

### 6.2 复验结果表（缺陷号 × 结果 × 证据）

| 复验项 | 场景 | 期望 | 实测 | 结果 |
|--------|------|------|------|:---:|
| **D-01** | 同 `transfer_no`、跨不同 Idempotency-Key 两次创建 | 第二笔 409 `TRANSFER_NO_DUPLICATE`，DB 仅 1 行 | 首笔 201；第二笔 **409 code=409**；同单号订单数 **1**；同键重放 409（幂等语义）；不同单号 **201**；绕过应用层直接插同单号 → 唯一索引拒绝（`重复键违反唯一约束"uq_recharge_orders_transfer_no"`） | ✅ 关闭 |
| **D-02** | reject 不传 note | 400 拦截 | **400** | ✅ |
| **D-02** | reject 传 note | 200；原 note 保留；`metadata.review_note` 独立；列表展示 | 200；note=`原始入账原因B` 保留；review_note=`凭证金额不符，驳回` 独立存储；GET 列表 failed 行 `review_note='凭证金额不符，驳回'` | ✅ 关闭 |
| **D-04** | 充值订单 reject 后查 audit_logs | 有 `recharge_order.reject` | audit 记录 action=`["recharge_order.reject"]` | ✅ 关闭 |
| **D-06** | 无余额行用户审核通过（建户） | audit 有 `balance.auto_create`，source='auto_create' | auto_create 审计 **1 条**，details=`{"source":"auto_create","user_id":…,"reference_type":"recharge_order","reference_id":"…"}` | ✅ |
| **D-06** | 已有余额行用户入账 | 不产生 auto_create 审计 | **0 条** | ✅ 关闭 |
| **P1-3** | finance GET /admin/recharge-orders | 200 | **200**；对照组 sales → 403 | ✅ 关闭 |

### 6.3 全量回归与类型检查（修复后）

```
pnpm --filter @3cloud/api exec vitest run --maxWorkers=4
Test Files  62 passed (62)
Tests       958 passed (958)
Duration    100.18s

api tsc --noEmit  →  0 错误
```

> 958 = 首轮 953 + 修复新增 5 用例（0028 唯一性/驳回必填/建户审计等），**零失败、零跳过**；D-07 环境观察未复现。

### 6.4 Gate 4 最终结论

**✅ 通过（Gate 4 放行）** —— 理由：
1. 全部缺陷关闭：P1（D-01）、P2（D-02）、P3 顺手项（D-04、D-06）复验通过；P3 观察项（D-03 留 R9、D-05 留二期、D-07 环境观察）为已记录口径差异/建议，非阻断；
2. 全量回归 62 文件 / 958 用例全过，typecheck 0 错误；
3. R1–R4 + D1 业务验收项全部满足（人工验收项 M-1~M-5 不变，留交付/调度）。

---

## 附：测试过程说明（合规声明）

- 未修改任何业务代码；临时补验脚本（V1–V7/N2、修复回归 R1–R5）仅用测试基建（独立用户/订单 + `app.inject`）验证行为，运行后已删除；
- 测试数据全部使用独立时间戳隔离并在 `afterAll` 清理；
- 未运行与并行进程冲突的长时间压测；全量 `pnpm test` 与 review-agent 无冲突（本次共执行三轮全量 + 一轮串行复跑 + 两轮补验脚本）。

*test-agent 2026-08-18 · Gate 4 验收测试报告（含修复回归）结束*
