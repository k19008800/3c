# 功能说明书：§7 非功能需求

> **对应文档**：[`PRD-非功能需求.md`](PRD-非功能需求.md)
> **深化参考**：[`ref-7-nfr.md`](ref-7-nfr.md)

---

### 功能描述

平台非功能需求覆盖性能、可用性、安全、可扩展性、兼容性、可维护性、数据合规 7 个维度。所有指标均有明确目标和当前状态验证。

### 完成能力

**性能指标：**

| 指标 | 目标 P95 | 当前状态 |
|------|---------|---------|
| 简单查询（单表/主键） | < 100ms | ✅ 已达标 |
| 列表分页（含筛选） | < 200ms | ✅ 已达标 |
| 复杂聚合（统计/趋势） | < 500ms | ✅ 已达标 |
| 模糊搜索（ILIKE） | < 100ms | ✅ 已达标 |
| 日志查询（分区表） | < 1s | ✅ 已达标 |
| 批量操作（万级） | < 3s | ✅ 已达标 |
| API 并发 | 1000+ QPS | ✅ 经四级限流保护 |

**可用性**：单机部署，PM2 cluster 自动重启，供应商故障自动熔断切换

**安全**：HTTP Only Cookie 鉴权、bcrypt API Key hash、CORS 配置、IP 黑白名单、2FA

**可扩展性**：API 无状态设计，数据库水平扩展预留（读写分离），前端组件化

**兼容性**：兼容 OpenAI API 格式，零代码迁移

**可维护性**：代码风格统一（DrizzleORM + Fastify + TypeScript），组件拆分完成

**数据合规**：个人电话脱敏、证件号脱敏、操作日志可审计、数据保留策略可配置

---

### [?] 页面帮助

**页面名称**：功能说明书：§7 非功能需求

**适用角色**：视具体功能而定（参见总览中的优先级和适用角色说明）

**功能定位**：该页面提供 功能说明书：§7 非功能需求 相关的配置、查询和管理能力。

**核心操作**：
1. 查看列表 / 详情
2. 创建 / 编辑 / 删除条目
3. 筛选 / 搜索 / 导出

**注意事项**：
- 部分操作涉及敏感数据，需二次确认或 2FA 身份验证
- 操作记录会写入操作日志

**常见问题**：
Q: 为什么某些操作不可用？
A: 请检查当前账号的权限角色是否包含对应操作权限。

### [?] 按钮级帮助对照表

| 按钮/操作 | 帮助说明 |
|----------|---------|
| 创建/新增 | 添加一条新记录 |
| 编辑 | 修改已有记录的字段内容 |
| 删除 | 删除选中的记录（不可恢复，需确认） |
| 搜索 | 按关键词搜索匹配的记录 |
| 筛选 | 按选中条件过滤列表 |
| 导出 CSV | 将当前列表数据导出为 CSV 文件 |
| 查看详情 | 查看选中记录的完整信息 |
