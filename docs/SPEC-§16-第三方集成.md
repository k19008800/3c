# 功能说明书：§16 第三方集成

> **📖 页面功能说明帮助**
>
> **页面用途**：第三方集成 功能说明书。本文档详细定义了该模块各功能点的字段规格、校验规则、边界条件、交互方式和注意事项。
>
> **适用角色**：开发人员、管理员
>
> **核心操作**：
- 了解第三方服务集成方式
- 查看各集成状态和配置方法
- 管理集成接入凭证和回调
>
> **注意事项**：本 SPEC 文档面向开发者和测试人员，包含字段级定义和校验规则。页面标题旁的 `[?]` 帮助气泡引用本文档内容，点击可查看当前页面功能说明。
>
> **关联组件**：页面标题右侧 `[?]` 帮助按钮，悬停/点击弹出帮助弹窗。




> **对应文档**：[`PRD-第三方集成.md`](PRD-第三方集成.md)

---

### 功能描述

平台与第三方系统的集成方案，包括支付网关、短信服务、对象存储、故障切换策略等。

### 完成能力

**16.1 第三方服务总览：**

| 系统 | 用途 | 状态 | 集成方式 |
|------|------|------|---------|
| 支付宝/微信支付 | 用户充值 | ✅ 已完成 | 支付回调 webhook |
| SMTP 邮件服务 | 通知推送 | ✅ 已完成 | 异步队列消费 |
| WebSocket | 实时推送 | ✅ 已完成 | 在线用户/告警推送 |
| 电子签章（法大大/上上签）| 合同签署 | 📋 P1 | API 对接 |
| 短信服务（阿里云/腾讯云）| 重要告警短信 | 📋 P1 | API 对接 |
| 对象存储（OSS/S3）| 文件/头像/合同存储 | 📋 P1 | SDK 集成 |
| 企业微信/钉钉 | 运营报表推送 | 📋 P2 | Webhook |
| IP 地理库 | IP 归属地解析 | ✅ 已完成 | 本地库/API |

**16.2 支付网关集成：**
- 支持支付宝和微信支付
- 配置项：APP ID、商户号、API Key、回调 URL
- 回调处理：异步接收支付结果 → 更新订单状态 → 通知用户
- 掉单处理：定时查询未完成订单状态

**16.3 短信服务集成：**
- 发送场景：验证码、重要告警通知
- 模板化管理：在平台后台创建短信模板
- 发送频率限制：同一手机号每分钟最多 1 条

**16.4 对象存储集成：**
- 用途：用户头像、合同文件、证书照片
- 配置：Bucket/Region/AccessKey/SecretKey
- 权限控制：仅平台后台可写，通过签名 URL 可读

**16.5 故障切换策略：**
- 第三方服务故障 → 记录错误日志 + 告警通知
- 支付网关故障 → 标记订单"充值中"，定时重查
- 短信故障 → 降级为邮件通知
- 所有第三方调用通过 adapter 模式封装，替换实现无需修改业务代码

---

### [?] 页面帮助

**页面名称**：第三方集成

**适用角色**：开发人员、管理员（运维）

**功能定位**：平台与第三方系统的集成方案总览与配置管理：支付网关、短信服务、对象存储、邮件、WebSocket、电子签章、故障切换策略等。

**子模块说明**：
- §16.1 第三方服务总览：各集成系统的用途/状态/集成方式一览
- §16.2 集成配置：各服务的接入凭证、回调地址配置
- §16.3 故障切换策略：第三方服务不可用时的降级与切换方案

**注意事项**：
- 支付回调使用 webhook 异步通知，需保证回调地址可达
- 第三方凭证属于敏感信息，配置操作需管理员权限并记录审计
- 部分集成（如电子签章）处于 P1 规划中，尚未上线

**常见问题**：
Q: 支付回调收不到怎么办？
A: 检查回调地址配置和签名验证逻辑，可查看回调日志定位问题。

Q: 第三方服务挂了会影响核心业务吗？
A: 核心计费链路有故障切换策略；非核心服务（如邮件）降级为队列重试。

### [?] 按钮级帮助对照表

**§16.2 集成配置**

| 按钮/操作 | 帮助说明 |
|----------|---------|
| 配置凭证 | 填写第三方服务的接入凭证（AppID/密钥等） |
| 测试连接 | 测试与第三方服务的连通性 |
| 查看回调日志 | 查看支付/短信等回调的处理记录 |
| 启用/停用集成 | 启用或停用某项第三方集成 |

**§16.3 故障切换**

| 按钮/操作 | 帮助说明 |
|----------|---------|
| 查看切换策略 | 查看各服务的故障降级与切换配置 |
| 手动切换 | 手动触发服务切换到备用通道 |


---

## 边界条件

| # | 场景 | 触发条件 | 预期行为 |
|---|------|---------|---------|
| THIRD-001 | 第三方 API 限流 | 短时内调用次数超过第三方服务商限流阈值（如支付网关 QPS > 100） | 采用本地令牌桶限流，超过阈值的请求排队等待或降级为异步处理，记录限流日志 |
| THIRD-002 | 支付回调超时 | 用户完成支付后，第三方支付网关回调接口超过配置超时（默认 30s）未响应 | 定时任务轮询未完成订单（每 2 分钟），调用第三方对账接口确认支付状态 |
| THIRD-003 | Webhook 重试耗尽 | 第三方 Webhook 推送失败后重试 3 次仍失败 | 标记 Webhook 为"死信"状态，持久化到死信队列，触发人工介入告警，支持手动重推 |
| THIRD-004 | 短信服务商切换 | 主短信服务商故障后自动切换到备选服务商 | 通过 adapter 模式无缝切换，切换期间请求排队等待（最长 5s），切换完成后自动恢复 |
| THIRD-005 | OSS 签名 URL 过期 | 用户通过已过期的签名 URL 访问存储文件 | 返回 403 Forbidden，前端引导用户重新获取签名 URL |
| THIRD-006 | 第三方凭证变更未同步 | 运营人员在第三方平台更换了 API Key/Secret，但未同步更新到 3cloud 配置 | 集成健康检查模块定时验证第三方凭证有效性，发现失效后发送告警通知 |
