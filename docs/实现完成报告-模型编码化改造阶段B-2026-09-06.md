# 3cloud 模型编码化改造 · 阶段 B 实现完成报告

- **日期**：2026-09-06
- **范围**：移除用户侧「供应商/渠道选择」→ 同一逻辑模型多供应商 = 系统内多条唯一「模型编码（model_code）」；编码规则后台可自定义，系统默认「厂商+模型」
- **状态**：后端 B1~B5 ✅ 前端 T1~T7 ✅ 未决事项 4/4 关闭 ✅

---

## 一、交付清单（文档）

| 文档 | 版本 | 说明 |
|------|------|------|
| PRD-模型编码化改造与去除用户供应商选择.md | v1.2 | M-C-01R 默认「厂商+模型」、M-C-07 模板变更边界、R-ADM-05/06 |
| PRD-…-补充1-编码规则自定义.md | v1.0 | 模板变量白名单 6 个、清洗/截断 200/冲突 -{seq} 规格 |
| SPEC-模型编码化改造与去除用户供应商选择.md | v1.2 | M-S-03R、S-C-1R、示例编码 vb-deepseek-v4-flash |
| 开发任务书-阶段B-前端模型编码化改造.md | — | T1~T6 + §3 契约 + 附录派发文本 |
| 开发任务书-阶段B-后端模型编码规则接口.md | — | B1~B5 + §3 接口契约 |
| _archive/ARCH-模型编码化改造-架构评审-2026-09.md | — | 架构评审存档 |

## 二、后端实现（B1~B5）

| 子任务 | 文件 | 内容 |
|--------|------|------|
| B1 编码生成核心 | `api/src/services/upstream/model-code.ts` | renderModelCode 纯函数（模板校验→渲染→清洗→冲突兜底）、generateModelCodeForSupplierModel（双 active 校验→UPDATE 落库） |
| B2 模板配置服务 | 同上 | get/setModelCodeTemplate（Redis 60s 缓存 + system_config 键 `model_code.template`）、validate、invalidate；默认 `{supplier_code}-{model_name}` |
| B3 admin 接口 | `api/src/routes/admin-model-codes.ts` + `app.ts` | GET 列表/筛/分页、PUT status、POST regenerate、GET/PUT rules、POST preview（6 接口，契约对齐任务书 §3） |
| B4 回填脚本 | `api/scripts/backfill-model-codes.ts` | 读模板、--dry-run/--template、幂等只填 NULL、与 B1 渲染规则一致 |
| B5 回归 | `admin-model-codes.test.ts` + `model-code.test.ts` | 18 + 43 用例 |

**验证结果**：

```text
api 全量测试   : ✅ 94 文件 / 1398 passed，既有 0 破坏
typecheck      : ✅ tsc --noEmit 0 errors
lint           : ✅ eslint 0 errors
回填 dry-run   : ✅ 默认「厂商+模型」格式，幂等
真实回填       : ✅ 124 行已填充，0 跳过；复检 NULL = 0
```

**公开接口收口**：`/public/models`、`/public/pricing` 响应补充 `model_code` 字段（营销站按编码展示闭环）。

## 三、前端实现（T1~T7）

| 子任务 | 文件 | 内容 |
|--------|------|------|
| T1 契约对齐 | `playground/types.ts`、`lib/channelization.ts`(删) | ModelRow 重写为编码化结构，删除旧 channels[] |
| T2 Playground 编码化 | `ModelCodeSelector.tsx` 等 | 编码输入+默认偏好+价格明细；6 Tab 请求体传 model_code |
| T3 日志按编码筛选 | `LogsPage.tsx` | model+provider 合并为编码筛选，旧数据「—」占位 |
| T4 下线渠道页 | `App.tsx`、`ConsoleLayout.tsx`、`VendorSelectorPage.tsx` | 删路由/入口，页面空壳 deprecated |
| T5 后台 UI | `adminModelCodes.ts`、`ModelCodeMaintenanceSection.tsx`、`ModelCodeRuleConfig.tsx` | 6 接口调用层（已切真实接口联调）、编码维护、规则配置 |
| T6 营销站 | web-portal models/pricing/PriceCalculator/i18n | 按编码展示，缓存读写价列 |
| T7 全量回归 | — | 见下 |

**T7 验证结果**：

```text
web-console typecheck/lint : ✅ 0 errors
web-console test           : ✅ 11 文件 / 68 用例全过，既有 0 破坏
web-console build          : ✅ 350 modules
web-portal typecheck/build : ✅ Next.js 编译成功（/models、/pricing）
术语残留 grep              : ✅ 用户侧活动 UI「供应商选择/渠道选择」0 残留
[?] 帮助                  : ✅ 新增/改动页面与按钮齐全（测试含断言）
```

## 四、未决事项处理结果（4/4 关闭）

| # | 事项 | 处理结果 |
|---|------|---------|
| 1 | 公开接口缺 model_code | ✅ 已补 `/public/models`（public.ts）与 `/public/pricing`（suppliers.ts） |
| 2 | 真实回填未执行 | ✅ 已执行：124 行填充、0 跳过；复检 `model_code IS NULL` = 0，幂等 |
| 3 | kb/3cloud 编码规范路径 | 已记录：规范位于工作区根 `kb/3cloud/`（项目外），本次以 PRD/SPEC/补充1 为权威依据 |
| 4 | 代码未提交 | ✅ 已按三批提交，见下 |

## 五、Git 提交记录（分支 feat/impersonation，工作区干净）

| Commit | 内容 | 文件数 |
|--------|------|--------|
| `6ae3bdc` | feat(api): 模型编码化主链（列/唯一索引/路由锁定/me.models 展开/默认偏好/公开接口字段） | 17 |
| `4b57019` | feat(api): 编码规则接口与回填 B1~B5 + PRD/SPEC v1.2 规格文档 | 14 |
| `09b8b66` | feat(web): 前端 T1~T6（选择器/日志/下线渠道页/后台 UI/营销站） | 28 |
| `59c4a48` | feat(api): /public/pricing 补充 model_code 字段收口 | 1 |

## 六、遗留说明（非阻塞）

- 迁移 0038 已在本地 DB 执行；其他环境部署时需 `pnpm db:migrate:manual` 或 drizzle 迁移。
- 回填脚本入口：`pnpm --filter @3cloud/api db:backfill:model-codes`（默认幂等，可 `--dry-run` 预览）。
- 编码模板后台可配置：admin UI 模型编码规则（`/admin/model-code-rules`），变更只对未生成编码的映射生效（M-C-07）。
