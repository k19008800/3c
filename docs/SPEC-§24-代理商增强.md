# 功能说明书：§24 代理商增强

> **对应文档**：[PRD-代理商体系.md]、[PRD-代理商支撑增强.md]
> **状态**：草案（仅需求文档）
> **优先级**：P1（邀请裂变、客户预警）、P2（素材库、排行榜、多级佣金、自定义定价）

---

## 24.0 总览

### 功能描述

在现有代理商体系（三级审核、佣金规则、提现双审）基础上，补充增长引擎、客户管理和多级扩展能力。

### 子模块清单

| 编号 | 模块 | 优先级 | 核心价值 |
|------|------|--------|---------|
| 24.1 | 代理商邀请裂变 | P1 | 代理可生成专属邀请链接，被邀请人注册后自动绑定为下级客户 |
| 24.2 | 代理商素材库 | P2 | 提供宣传海报/文案/PPT 等素材，代理可直接下载使用 |
| 24.3 | 代理商业绩排行榜 | P2 | 月度/季度排名，激励竞争 |
| 24.4 | 代理商客户预警 | P1 | 客户余额不足/调用量骤降时主动通知代理进行客户关怀 |
| 24.5 | 代理商多级佣金 | P2 | 支持二级佣金（下级代理佣金上级抽成） |
| 24.6 | 代理商自定义定价 | P2 | 代理可为其名下客户设置不同的价格倍率 |

---

## 24.1 代理商邀请裂变

### 功能描述

代理商可生成专属邀请链接/二维码，客户点击链接注册后自动绑定为该代理的下级客户。代理端可追踪每个邀请链接的转化效果（曝光/点击/注册/消费）。

### 完成能力 / 展示效果

**代理端邀请页：**

```
我的专属邀请链接
  ┌────────────────────────────────────────────┐
  │  邀请链接:                                   │
  │  https://unmisa.com/register?agent=AG001    │ [复制]
  │                                              │
  │  邀请二维码:                                  │
  │  [QR Code]                                   │ [下载]
  │                                              │
  │  已邀请: 25 人 | 已注册: 18 人 | 已消费: 8 人 │
  └────────────────────────────────────────────┘

  邀请效果统计（近 30 天）
  日期      曝光  点击  注册  消费总额
  07-28     120    15    3    ¥234.00
  07-27     98     12    2    ¥156.00
  ─────────────────────────────────
  合计      2,340  320  18   ¥4,560.00
  转化率:    —     13.7% 5.6%  —
```

### 数据表扩展

```typescript
// agent_referral_links — 代理邀请链接
export const agentReferralLinks = pgTable("agent_referral_links", {
  id: serial("id").primaryKey(),
  agentId: integer("agent_id").notNull().references(() => users.id),
  code: varchar("code", { length: 20 }).notNull().unique(),  // AG001 等
  customName: varchar("custom_name", { length: 100 }),       // 自定义链接名称
  clickCount: integer("click_count").default(0),
  registerCount: integer("register_count").default(0),
  createdAt: timestamp("created_at").defaultNow(),
});

// users 表扩展字段
// referred_by_agent: integer — 被哪个代理邀请注册
```

### API 接口

```
GET  /api/v1/agent/referral/links               — 邀请链接列表
POST /api/v1/agent/referral/links               — 生成新邀请链接
GET  /api/v1/agent/referral/stats               — 邀请效果统计
GET  /api/v1/agent/referral/clients             — 邀请注册的客户列表及消费

// 注册接口扩展（已存在）
POST /api/v1/auth/register — 支持 ?agent=AG001 参数
```

### 验收标准

#### □ 流程图一致性校验 — 与对应流程图对比验证流程分支、异常处理、决策节点完全一致

1. 代理生成专属邀请链接 → 发送给客户 → 客户注册后自动绑定为该代理的下级
2. 代理看到邀请统计（曝光/点击/注册/消费）
3. 代理可生成多个邀请链接用于不同渠道
4. 注册时携带 agent 参数 → 用户 profile 标记 referred_by_agent

---

## 24.2 代理商素材库

### 功能描述

管理后台提供素材上传和管理，代理端可下载宣传海报、文案模板、产品介绍 PPT 等素材，方便代理推广平台。

### 完成能力 / 展示效果

**管理后台素材管理：**

```
素材管理
  [上传素材]  [分类管理]

  分类: [全部 ▼]

  3Cloud 产品介绍 PPT    PPT  2.3MB  2026-07-25  [编辑] [删除]
  注册引导海报           PNG  1.1MB  2026-07-20  [编辑] [删除]
  定价对比文案            DOCX 45KB  2026-07-18  [编辑] [删除]
```

**代理端素材列表：**

```
推广素材
  素材名称          类型  大小    更新日期    下载量
  产品介绍 PPT      PPT   2.3MB  07-25      128次  [下载]
  注册引导海报      PNG   1.1MB  07-20      89次   [下载]
  定价对比文案      DOCX  45KB   07-18      56次   [下载]
```

### 数据表结构

```typescript
// agent_materials — 代理商素材
export const agentMaterials = pgTable("agent_materials", {
  id: serial("id").primaryKey(),
  title: varchar("title", { length: 200 }).notNull(),
  description: text("description"),
  type: varchar("type", { length: 20 }).notNull(), // ppt / png / docx / pdf / link
  category: varchar("category", { length: 50 }),
  fileUrl: varchar("file_url", { length: 500 }),
  fileSize: integer("file_size"),               // bytes
  downloadCount: integer("download_count").default(0),
  status: varchar("status", { length: 20 }).default("published"),
  createdAt: timestamp("created_at").defaultNow(),
  updatedAt: timestamp("updated_at").defaultNow(),
});
```

### 验收标准

1. 管理员上传素材 → 代理端可看到并下载
2. 素材带下载量统计
3. 管理员可编辑/删除素材

---

## 24.3 代理商业绩排行榜

### 功能描述

月度/季度代理商业绩排名，激励竞争。可设置排名奖励机制（额外佣金比例/一次性奖金）。

### 完成能力 / 展示效果

```
代理商排行榜 — 2026 年 7 月
  排名  代理商          客户数  月消费总额      佣金   上月排名
  🥇   张三团队          128    ¥128,000.00   ¥6,400  🥇
  🥈   李四科技          96     ¥89,000.00    ¥4,450  🥉
  🥉   王五AI            72     ¥65,000.00    ¥3,250  🥈
  4     赵六工作室       55     ¥42,000.00    ¥2,100  —
  5     钱七云服务       41     ¥31,000.00    ¥1,550  4
```

### 奖励配置（运营后台）

```
排行榜奖励规则:
  第 1 名: 当月额外佣金 10%
  第 2-3 名: 当月额外佣金 5%
  第 4-5 名: 一次性奖励 ¥500
```

### API 接口

```
GET /api/v1/agent/ranking?period=month&date=2026-07
GET /api/v1/admin/agent-ranking-config     — 奖励规则配置
PUT /api/v1/admin/agent-ranking-config     — 更新奖励规则
```

### 验收标准

1. 代理端显示月度排名和上下月排名变化
2. 排名靠前的代理当月佣金按照奖励规则自动计算
3. 运营后台可配置奖励规则

---

## 24.4 代理商客户预警

### 功能描述

代理名下客户出现异常时（余额不足 / 调用量骤降 / 即将到期），平台主动通知代理进行客户关怀，减少客户流失。

### 预警类型

| 预警类型 | 触发条件 | 通知内容 |
|---------|---------|---------|
| 余额不足 | 客户余额 < ¥10 | "您的客户张三余额仅剩 ¥3.50，建议提醒充值" |
| 调用骤降 | 日调用量较 7 日均值下降 > 80% | "您的客户李四 API 调用量骤降 85%，是否联系了解情况？" |
| 余额即将耗尽 | 按当前速度 3 天内余额耗尽 | "客户王五余额预计 2 天后耗尽，建议提前联系续费" |
| 客户流失 | 连续 14 天无调用 | "客户钱七已 14 天未使用，请确认是否流失" |

### API 接口

```
GET /api/v1/agent/alerts/clients     — 客户预警列表
PUT /api/v1/agent/alerts/:id/dismiss — 标记已处理
```

### 验收标准

1. 代理端显示名下客户的预警列表
2. 预警触发时代理收到站内通知
3. 代理可标记预警为"已处理"

---

## 24.5 代理商多级佣金

### 功能描述

当前仅支持一级佣金。扩展为支持二级：代理商发展下级代理，下级代理的佣金按比例抽成给上级。

### 佣金抽成规则

```
├── 一级代理: 直接发展客户的消费 → 按佣金规则结算
├── 二级代理: 上级代理 50% 的佣金中抽成 20%
│   └── 例: 二级代理佣金 ¥100 → 上级代理额外获得 ¥20
└── 运营后台配置: 上级抽成比例（默认 20%）
```

### 验收标准

1. 上级代理可查看名下所有下级代理的业绩汇总
2. 下级代理结算佣金时，上级自动获得对应抽成
3. 运营后台可配置抽成比例

---

## 24.6 代理商自定义定价

### 功能描述

代理可为其名下客户设置不同的价格倍率，类似"渠道定价"。代理需要更高的利润空间时，可在平台基础价格上上浮一定比例。

### 规则

```
├── 平台基础价格: ¥0.50 / 1K tokens（deepseek-chat）
├── 代理可设置倍率: 1.0x ~ 2.0x
│   └── 1.2x → 客户实际价格: ¥0.60 / 1K tokens
├── 代理利润: 客户支付价格 - 平台基础价格 = 代理利润空间
└── 运营后台可设置代理倍率上限（默认 2.0x）
```

### 验收标准

1. 代理可为名下客户单独设置价格倍率
2. 不同客户可设置不同倍率
3. 客户在计费时按代理设置的价格倍率计算
4. 倍率上限由运营后台控制


---

### [?] 页面帮助

**页面名称**：功能说明书：§24 代理商增强

**适用角色**：视具体功能而定（参见总览中的优先级和适用角色说明）

**功能定位**：该页面提供 功能说明书：§24 代理商增强 相关的配置、查询和管理能力。

**核心操作**：
1. 查看列表 / 详情
2. 创建 / 编辑 / 删除条目
3. 筛选 / 搜索 / 导出

**注意事项**：
- 部分操作涉及敏感数据，需二次确认或 2FA 身份验证
- 操作记录会写入操作日志

**常见问题**：
Q: 为什么某些操作不可用？
A: 请检查当前账号的权限角色是否包含对应操作权限。

### [?] 按钮级帮助对照表

| 按钮/操作 | 帮助说明 |
|----------|---------|
| 创建/新增 | 添加一条新记录 |
| 编辑 | 修改已有记录的字段内容 |
| 删除 | 删除选中的记录（不可恢复，需确认） |
| 搜索 | 按关键词搜索匹配的记录 |
| 筛选 | 按选中条件过滤列表 |
| 导出 CSV | 将当前列表数据导出为 CSV 文件 |
| 查看详情 | 查看选中记录的完整信息 |
